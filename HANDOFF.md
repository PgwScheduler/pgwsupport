# PGW Support Portal — Session Handoff

Recap of everything done so far, for picking up in a fresh Claude Code chat.
See `CLAUDE.md.txt` in this same folder for the original project brief —
that's still the source of truth for business rules (deposit-sheet math,
org hierarchy, roles, ground rules). This file just tracks what's been
*built* against that brief.

## Where things live

- **Frontend app**: `pgw-portal/` — Vite + React + Tailwind, deployed to Vercel
- **Database migrations**: SQL files in the repo root, numbered `01`–`47` (plus `38a`, `39a`, `39b`, `39c`), run in order in the Supabase SQL Editor. All have been applied and verified.
- **GitHub repo**: `https://github.com/PgwScheduler/pgwsupport` (branch `main`)
- **Production URL**: `https://pgwsupport.vercel.app` (temporary Vercel URL — custom domain `pgwsupport.com` deliberately not configured yet, per instruction)
- **Local dev**: `cd pgw-portal && npm install && npm run dev` (needs `pgw-portal/.env` with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` — see `.env.example`)

## Database migrations (all applied, in order)

1. `pgw_portal_schema_01.sql` — base tables, RLS, storage bucket, new-user trigger (pre-existing before this session)
2. `pgw_portal_regions_upgrade_02.sql` — regions/districts hierarchy, `can_access_location()` (pre-existing)
3. `pgw_portal_seed_3.sql` — 3 regions, 5 districts, 36 stores (pre-existing)
4. `pgw_add_store_number_04.sql` / `pgw_populate_store_numbers_05.sql` — store numbers (pre-existing)
5. `pgw_add_drawer_float_06.sql` — `locations.drawer_float`, defaults $200, $400 for store #3935 (Two Notch)
6. `pgw_employee_hours_weekly_07.sql` — rewrote `employee_hours` from daily to weekly (mon–sat columns + `hours_turned`); RLS lets store managers update/delete their own store's rows (not just master)
7. `pgw_cash_drawer_rewrite_08.sql` — rewrote `cash_drawer_closeouts` to store the full deposit sheet (denomination counts as jsonb, all sales-summary fields, 4 variable-length line-item arrays as jsonb). Computed totals are **never stored**, only derived on read.
8. `pgw_training_library_09.sql` — new `training` table + storage bucket, company-wide (no `location_id`), everyone reads, admin/master write
9. `pgw_documents_folders_10.sql` — added `parent_id` + `item_type` to `documents` for nested folders
10. `pgw_drop_daily_closeouts_11.sql` — dropped the unused `daily_closeouts` table
11. `pgw_employee_hours_notes_12.sql` — added a free-text `notes` column to `employee_hours` (e.g. "covering from store #3936")
12. `pgw_profiles_email_13.sql` — mirrors `auth.users.email` onto `profiles.email` (via the signup trigger + a backfill), so the User Management screen can list users without needing Supabase's admin API
13. `pgw_employee_hours_payroll_rebuild_14.sql` — replaced the weekly `employee_hours` grid with the full payroll model: `employees`, `timesheet_entries` (store-visible) and the pay tables `employee_pay_rates` + `timesheet_pay` (**master/admin-only RLS — a store SELECT returns zero rows**). Pay is derived on read; stores see only percentages via the `payroll_pct_summary()` SECURITY DEFINER function. `employee_hours` dropped.
14. `pgw_flat_flags_for_store_15.sql` — flat-rate-vs-hourly flag helper used by the payroll percentage view.
15. `pgw_speedee_brand_16.sql` — made payroll brand-aware. Added **`locations.brand`** (`'midas'`/`'speedee'`, default midas) and set stores **3009/3025/3029/3308 → speedee**; split `timesheet_entries` into shared core + `timesheet_midas`/`timesheet_speedee`; added `brand_settings`, `role_sales_rates`, `store_week_sales`, and the `payroll_speedee_summary()` RPC.
16. `pgw_employee_schedules_17.sql` — `employee_schedules` (per-store shift calendar, timezone-naive `date`+`time`, no overnight shifts). Access via `can_access_location()`; references only the `employees` roster, never pay data.
17. `pgw_daily_tic_sheet_18.sql` — **Daily Tic Sheet.** Reference tables `service_categories` + `brand_service_categories` (seeded with the 29 Midas categories, `display_order` 10..290; `horizon_key` values are the external system's exact field names). Data tables `daily_kpi` (one row per store per `business_date`) + `daily_service_units`. Uses `location_id uuid → locations(id)` (there is no bigint `stores` table); does **not** re-add `brand` (already present from migration 16). RLS mirrors the cash-drawer tables — read/insert/update via `can_access_location(location_id)`, delete master-only, child units resolved through the parent row; the two reference tables are readable by all authenticated users and **master-only** to write.
18. `pgw_speedee_service_categories_19.sql` — **SpeeDee tic-sheet categories** (data only, no schema change). Adds two new `service_categories` (`kpi_su_axels_shafts` "Axels & Shafts", `kpi_su_tire_rotation` "Tire Rotation") and 31 `brand_service_categories` rows for `brand='speedee'` (`display_order` 10..310, a different roughly-alphabetical order with the two new lines slotted in, not appended). Leaves the 29 Midas rows untouched; the two new categories map to SpeeDee only. The entry screen picks these up with no code change (it filters by the location's brand).
19. `pgw_store_goals_20.sql` — **Store goals (2026).** `holidays` (5 closures/yr, 2026+2027), `derived_days_open(year,month)` / `annual_days_open(year)` (308 working days in 2026, never hardcoded), `store_annual_goals` (36 stores) + `store_monthly_goals` (cars-per-day + optional day-count override; 324 rows / 27 stores — nine Model-B stores have none), and the derived view `v_store_monthly_gp_target` (annual ÷ working days in year × working days in month). Built from the supplied `step2_goals_seed.sql` unchanged in values; **added on top**: RLS (goals read via `can_access_location`, write admin/master; `holidays` read-all, master-write), `security_invoker = true` on the view so a store user reading it is scoped by the base-table RLS (else it would leak every store's targets), and idempotency guards.
20. `pgw_tic_sheet_grid_21.sql` — **Tic-sheet category fixes + `first_time_customers`.** Renames in the shared `service_categories`: `kpi_su_ac_heat` → "A/C Refresh", `kpi_su_engine_performance` → "Catalytic Converters" (shared table, so SpeeDee shows the corrected names too — correct, it's the service's real name; SpeeDee's *list* is untouched). New category `local_su_15k_critical_sys` ("15K Critical Sys Treatment"), Midas only, slotted after Fuel Injection Flush — the `local_` prefix marks a key with **no Horizon field**, so downstream upload code must not assume every key maps to Horizon. Midas renumbered to **30** categories in tens. Adds `daily_kpi.first_time_customers int`.
21. `pgw_daily_kpi_sales_breakdown_22.sql` — adds `daily_kpi.sales_supplies` + `sales_groupon` (Parts/Tire cost already existed). Grid **Sales** = labor+parts+tires+supplies+groupon+discounts; **Gross profit** (goals strip "GP before labor") = Sales − cost_parts − cost_tires.
22. `pgw_daily_kpi_discount_sign_23.sql` — data-only: `sales_discounts` is stored **negative** (a discount reduces Sales). Flips any rows entered as positive (`set sales_discounts = -abs(sales_discounts) where > 0`; idempotent). The Sales panel coerces the entry to negative on save; Sales/GP sum the stored value, so no formula change.
23. `pgw_tech_time_tracker_24.sql` — **Technician Time Tracker.** Nine **slots** per store (`tech_slots`, not nine employees — a slot holds an employee, a placeholder label, or is empty and contributes zero). `tech_daily` (hours/flag/labor per slot per day, store-visible) and the **master/admin-only** `tech_weekly` (weekly `other_pay`) + effective-dated `tech_pay_rates` (`flat_rate` $/flag-hr, `guarantee_rate` $/hr-worked). Pay is **derived, never stored**; weeks run **Sun–Sat**, a month = the five Sun–Sat blocks from the Sunday on/before the 1st. Two SECURITY DEFINER functions return **store-level aggregates only** and are store-callable: `tech_store_month` (labor sales, labor cost, flag/hours, avg tech cost/sold-hr, shop proficiency) and `tech_store_daily` (per-day labor sales for the tic sheet + the **defective** daily allocation for Horizon). The internal `_tech_days` helper emits per-slot pay so it is `REVOKE`d from `PUBLIC`; per-technician pay stays master/admin-only. `tech_pay_rates.flat_rate` is **authoritative** for a tech's flat rate — a trigger (`sync_tech_flat_rate`) mirrors the current effective value into `employee_pay_rates.flat_rate_per_hour`, and the Payroll flat field is read-only for techs. The daily allocation **intentionally over-allocates** (a flag-hours/zero-hours day collects a surplus share; Millwood wk3 allocates $913.15 vs true $896.08) to preserve Horizon continuity — `total_pay` is correct, `daily_allocation` is the defect, both retained. Seed: `pgw_seed_millwood_july_tech.sql` (store #3303). Verified live against `Millwood July 1.xlsm` to the penny (store labor cost $15,640.19, month pay $3,905.20, etc.).
24. `pgw_tic_sheet_totals_25.sql` — **Tic sheet totals, self-correcting goals, zero dollar tickets.** Swaps the unused `daily_kpi.first_time_customers` for **`zero_dollar_tickets`** (daily entry, Summary column F in the source; **no Horizon field** — the upload builder must skip it, the second such field after `local_su_15k_critical_sys`). Zero dollar tickets do **not** reduce `ro_count`, so ROs / capture rate / average estimate per car are untouched. Drops any check constraint mentioning `sales_discounts` so discounts are **signed both ways** (Millwood July: −125.00, −396.53, **+107.00**, netting −1,997.91) — migration 23's blanket `-abs()` flip is **superseded and must not be re-run**. Two new goal tables: **`store_tic_goals`** (per store, `zero_dollar_ticket_pct` default 0.10 — the task called it `store_goals`, renamed to sit beside `store_annual_goals`/`store_monthly_goals`) and **`store_category_goals`** (per store per category, `goal_pct_of_cars` + `average_sale`, seeded from the workbook's company template for every store × its brand's list). Both read via `can_access_location`, write admin/master. Seed: `pgw_seed_millwood_july_kpi.sql` (Millwood #3303, July 2026 — 26 days of ROs / zero-dollar tickets / sales lines / declined / units; `sales_labor` deliberately left 0 because the grid reads labor from `tech_store_daily`).
25. `pgw_bonus_tracker_26.sql` — **Bonus Tracker, four models.** `bonus_plans` (which model a store is on), `bonus_monthly_targets` (days open, car goal, sales goal, GP budget, gold/silver/bronze, **`last_year_gp`**), `bonus_incentive_tiers` (`tire` / `credit_app` / `car_increase`, with `increment_above` on the row that *anchors* the overage rule), `bonus_monthly_inputs` (five-star reviews, phone conversion, Model D's referral GP credit — **nullable on purpose**, null means never entered) and `bonus_flags` (the nine open policy questions, surfaced on screen). **Model D is a fourth structure**, not a variant: SpeeDee Lexington #3308 pays a flat 3% of GP to the store manager with a car-increase-vs-last-year bonus and the Midas referral credit. Because the task demanded *no* thresholds/percentages/scales in code, the model rates live in data too — `bonus_model_rates` (A 7/5/4%, B 3/1.5% manager + 1% assistant, C 4.55/3.25/2.60%, D flat 3%), `bonus_model_splits` (A: GM 65 / Assistant 25 / Advisor 10) and `bonus_policy` ($10 a review, 15-review minimum, $75 penalty, 35-app floor, 40% phone conversion, Model C's $1,000 cap, Model B's 6% improvement). Store tables read via `can_access_location` and write admin/master; the three company-wide tables read to all, write master. Seed `pgw_seed_bonus_2026.sql`: 36 plans (23 A / 9 B / 3 C / 1 D), 432 monthly targets, 232 incentive tiers. **Store names were matched to `store_number` by exact Yearly GP against migration 20's `store_annual_goals`** — all 36 matched uniquely, which is what settles the two non-obvious short names (`Lex` = Lexington Midas #3276, `Wesmark` = #3938). Four handout sheets (Rabon, SD Bush, Broad, PGW North roll-up) have no store here and are not seeded. Note: after running this, PostgREST's schema cache may need `notify pgrst, 'reload schema';` before the new tables are visible to the app.
26. `pgw_bonus_store_reviews_27.sql` — **store managers log their own five-star review count.** Migration 26 had `bonus_monthly_inputs` on admin/master write like the other goal tables, which meant a manager had to phone an admin to record a number only the store can count. Insert/update now go through `can_access_location` (delete stays admin/master). The table is **not** simply opened up: a `before insert or update` trigger (`bonus_inputs_column_guard`) keeps the two money-moving columns admin/master — **`referral_gp_credit`** adds straight into the gross profit the bonus is calculated on, so a Model D store that could type its own number could inflate its own payout, and `phone_conversion_pct` waives the Model A credit penalty. A store's insert gets both forced to their defaults; an update that changes either is refused with `42501`, the same code RLS raises, so the app surfaces it identically. The trigger runs as **invoker** — `current_user_role()` is already SECURITY DEFINER (migration 01), so it needs no elevated rights of its own. Verified live as a store user: logging and editing a review count works, setting the referral credit or phone conversion is refused, and smuggling the referral credit alongside a legitimate review edit rolls the whole statement back (the review change does not sneak through). The client sends only `google_reviews` for a non-admin, so a store save can never trip the guard on a field it did not touch.
27. `pgw_bonus_wesmark_fix_28.sql` — **corrects Wesmark's (#3938 Midas Sumter) corrupt 2026 targets.** `bonus_seed_2026.csv` is column-shifted for this one store: its handout carries three columns the standard Model A sheet does not (a *2025 Sales* column and two *"vs 25"* comparison columns), and the CSV was read positionally, so every value after `sales_goal` landed one column left. `gp_budget` got 2025 Sales, `gold` got the GP Budget, **`silver` got a "vs 25" ratio — $0.26** — and `bronze` got Gold. The effect: any month with more than twenty-six cents of projected GP resolved to Silver and paid 5%, while bronze at $88,214 sat *above* silver and was unreachable; a strong month clearing the real Gold of $88,214 was meanwhile held at Silver by the corrupt $92,857. Wrong in both directions on a store with a $1.1M annual GP budget. All twelve months are restored from the handout's own columns (F/H/J/K) and sum to **1,100,000**, exactly the Yearly GP the sheet states — the check that they are the right columns. **Wesmark's bronze is 85% of budget, not the 80% every other store uses** — that is the handout as written (`K = F * 0.85`), preserved and flagged (`wesmark_bronze_85`). Also adds a `bonus_targets_tier_order` CHECK constraint requiring `gold >= silver >= bronze` where both sides are present, which would have rejected the bad rows at seed time; Model B's null bronze and equal gold/silver in a floored month still pass. Every other store was clean — 312 of the 324 non-Model-B rows are exactly 95/90/80 and the other twelve were Wesmark's.
28. `pgw_tech_slot_management_29.sql` — **Tech Tracker slot management, and the history fix it required.** `tech_daily` referenced only `tech_slot_id`, and `_tech_days` resolved pay rates by joining `tech_slots.employee_id` — the slot's *current* occupant. Reassigning a slot would therefore not merely relabel past days but **re-cost** them at the incoming technician's rates, flowing labor cost → gross profit → the goals strip → the bonus tracker; a reassignment in August would have silently changed July's bonus payout. Fix: **`tech_daily.employee_id`**, stamped at insert by the `tech_daily_stamp_employee` trigger from whoever holds the slot at that moment and never rewritten by a reassignment; `_tech_days` now resolves rates from `td.employee_id`. Existing rows backfilled from current occupancy (valid only because no slot was reassignable before this). Considered and rejected: a slot-assignment history table with effective date ranges — it models the same fact indirectly and makes every read temporal. Reassignment goes through the **`tech_reassign_slot`** RPC so the slot move and the day re-stamp are one transaction; it takes an **effective date** and re-stamps only rows on/after it, returning the count so the UI can preview it before the admin confirms. SECURITY DEFINER, so it re-checks role **and** `can_access_location` itself. Also: partial unique index `tech_slots_one_slot_per_employee (location_id, employee_id) where employee_id is not null`, and `tech_slots` insert/update tightened from `can_access_location` to **admin/master** (a store user could previously rewrite slots through the API even though the UI hid the controls).

29. `pgw_shift_types_30.sql` — **Shift types + month duplication (Employee Schedule).** Company-wide `shift_types` catalog (everyone reads, admin/master writes — 36 stores inventing their own colours makes cross-store review unreadable), seeded with seven: Regular, PTO, UTO, Sick, Training, Holiday, Open/Unassigned. `employee_schedules.shift_type_id` is **nullable** and a null means an ordinary worked shift that renders exactly as before. `counts_toward_hours` drives the weekly totals row; `is_copyable` governs duplication. Two columns beyond the task's DDL: **`export_argb`**, because the screen palette and the Excel palette cannot be the same (screen tokens clear AA on near-black, workbooks are light with black text per index.css), and a `char_length(abbreviation) <= 4` check. Duplication is `schedule_copy_month(location, source, target, mode, commit)` over `_schedule_copy_plan` — **preview and commit read the same plan function, so the summary a user confirms cannot drift from what runs**. Alignment carries each shift's offset in DAYS from the Monday opening the source month's grid onto the Monday opening the target's; that single offset already encodes (week index, weekday), so a Monday lands on a Monday with no special-casing. Verdicts are mutually exclusive: `time_off` / `inactive` / `overflow` / `existing` / `create`. Replace clears the target month first and is **admin/master only**; Fill-empty stays open to anyone `can_access_location` admits, preserving the district/regional edit right from migration 17. The whole copy is one plpgsql body, so a failure rolls back the delete and every insert together.

30. `pgw_open_shifts_31.sql` — **open shifts need no person.** Migration 30 added an "Open / Unassigned" type to a table whose `employee_id` was NOT NULL, so an open shift had to be parked against a real person. Dropping the constraint is one line; **three things break with it and are fixed here.** (1) The copy plan would have skipped every open shift — its verdict reads `when e.id is null or e.active = false then 'inactive'`, and a null `employee_id` makes the left join produce a null `e.id`, so every open shift would have been reported as *"skipped — employee no longer active"* with a null in the names array. The inactive test now applies only when an employee is actually named. (2) Fill-empty would have duplicated them on every run, because the collision test is `x.employee_id = s.employee_id` and `NULL = NULL` is never true — re-running a fill copy is a no-op today and that property would have died silently. Unassigned shifts now collide on `(date, start_time)`. (3) `employee_schedules_uniq` stops constraining unassigned rows, since Postgres treats NULLs as distinct — **left as-is deliberately**, because "we need three people Saturday morning" is expressed as three identical open shifts; duplication-on-copy is handled by (2). Adds `employee_schedules_person_or_type` (`employee_id is not null or shift_type_id is not null`) so a row can never be a blank block with no name and nothing to label it. `employee_id` keeps ON DELETE CASCADE — deleting an employee still removes their shifts rather than turning them into open ones, which is a separate decision.

31. `pgw_payroll_daily_sunday_32.sql` — **Payroll: daily entry, Sunday week, technician hours from the Tech Tracker.** Three changes that only work together. The pay week moves Monday → **Sunday**, matching the tic sheet, tech tracker and bonus math — payroll was the one screen out of step, which put every Sunday in a different week depending on the screen and made payroll-to-sales meaningless. Hours are captured **daily** (`payroll_daily`, unique on `(employee_id, work_date)`, plus `hours_worked_other` keeping the existing "clocked at another store" split rather than widening the key, and `hours_turned` for a uniform grain). **Overtime stays weekly** — one 40-hour threshold on the week's total, never per day. Cutover **2026-08-30**, company-wide, in a single-row `payroll_config` table because a CHECK cannot reference a table, so every rule depending on it is a trigger reading it; nothing is backfilled, since daily detail cannot be recovered from a weekly total. `payroll_config.include_technicians` makes the brief's "one-line change" a one-field change with no deploy. **Both weekly tables flip together** — `timesheet_entries` and `store_week_sales` take `check (dow in (0,1))` plus a shared trigger (Sunday from the cutover, Monday before it); a straight flip to `dow = 0` is impossible while any Monday row exists, and they must move on the same date because `store_week_sales` is the SpeeDee percentage's denominator. Pre-cutover hours are frozen (master keeps an escape hatch); post-cutover weekly hour columns must stay zero or they would be counted by nothing. **One entry per person per day is enforced in both directions** by a trigger on each table — a unique index cannot span two, and neither side silently wins: a collision is refused by name so a human decides. Placeholder slots (`tech_daily.employee_id` null) are exempt. The `tech_daily` guard is an **AFTER** trigger on purpose, because `tech_daily_stamp_employee` (migration 29) is a BEFORE trigger filling `employee_id` and a BEFORE guard would race it on name order. **`payroll_day_hours`** resolves each (person, **day**) to one source, so someone who is a technician for part of a period needs no special case and still gets one weekly overtime threshold. **`payroll_pct_summary` and `flat_flags_for_week` rewritten** to resolve hours through the cutover and to drive off the **roster** rather than `timesheet_entries` — post-cutover an employee can have daily hours and no weekly row, which the old query would have dropped silently. **Salary now keys off `employees.is_store_manager`, not `position = 'manager'`** (one per location, CHECK-constrained to a manager row): the old rule paid every `manager` row a salary and swept assistant managers in with the GM, and BDC's rule — only the GM excluded, assistants stay in — is only coherent if assistants are hourly. **`payroll_to_sales_wtd`** returns window bounds, both wage components, gross sales, the ratio, the technicians flag, `missing_store_manager` and `unattributed_days`; numerator excludes the store manager entirely (not their wages, not their hours, not their day in the window bound) and never adds bonuses or incentives, technicians come in at the Tech Tracker engine's figure with `other_pay` kept, denominator is the tic sheet's own Sales (Groupon excluded per migration 25), and **the window is the intersection** of the days both sides have. Two review catches: `LEAST` ignores nulls, so `least(hours_thru, sales_thru)` would have divided a full week of wages by nothing when one side was empty; and migrations 14/16 both name a CTE column `actual_sales` alongside the OUT parameter of the same name, a latent **42702** ambiguity not copied here.
32. `pgw_dashboard_range_metrics_33.sql` — **Dashboard widgets over an arbitrary date range.** Four functions. **`_tech_pay_range`** (internal, revoked from public) is the range twin of the tech pay engine: it evaluates every WHOLE Sunday–Saturday week a range touches and attributes pay into the range by the share of that week's HOURS falling inside it, so a range covering half a week gets half that week's pay **at full-week rates** and never a half-week overtime calculation. **`tech_store_range`** is its store-callable wrapper, matching `tech_store_month`'s columns exactly — verified identical on Millwood July (labour cost $15,640.19, labour sales $58,582.50, 370.33 flag hours). **`dashboard_range_metrics`** returns the five widgets' figures in one call; **`payroll_to_sales_range`** generalises the Task 8 metric from one week to any span, keeping every rule (store manager excluded entirely, bonuses and incentives never added, technicians at the engine's figure subject to `payroll_config`, both sides over the identical window). **Scoping is done in the database**: `loc` null aggregates across everything `can_access_location()` admits the caller to, which is what lets one dashboard serve a store, district, regional and master with no branching. **Uses the TIC SHEET definition of Sales** (Groupon excluded, migration 25) throughout, per BDC, so the Sales widget, GP% and payroll-to-sales all divide by one number; `groupon` is returned separately so the Summary R35 figure stays one addition away. **Three bugs caught before shipping:** `service_categories.id` is `bigint` not uuid (only locations and employees are uuid-keyed), so the first version failed outright; `sum()` over the `int` columns `credit_apps`/`ro_count`/`units` returns **bigint**, which made `tire_units / days_with_data` **integer division** — 95 tires over 9 days would have silently read 10 rather than 10.56; and because "structure of query does not match function result type" names no column, every returned column is now **cast explicitly** rather than left to inference.
33. `pgw_revoke_internal_helpers_34.sql` — **SECURITY FIX: a store user could read individual technician pay.** `_tech_days` was reachable by any signed-in store user and returned PER-SLOT, PER-DAY `guar_pay`, `commission` and `guar_rate`. `tech_slots` is store-visible, so every row was attributable to a named technician; `guar_rate` handed over their guarantee rate outright and `commission / flag` yields their flat rate. Confirmed live as `teststore@pgwsupport.com` (role `store`, #3303): 83 rows including `guar_rate: 16`. **Root cause:** `revoke all on function ... from public` revokes the PUBLIC pseudo-role, but Supabase separately grants EXECUTE to `authenticated` and `anon`, so the statement was a **no-op** — present since migration 24 and repeated in 29, 30, 31 and 33. The tell was visible all along: `schedule_copy_month` carries the same revoke and the app calls it on every month duplication, so if it had bitten the feature would have been broken from day one. **The fix is deliberately narrow.** `_tech_days` and `_tech_pay_range` are revoked from `anon` and `authenticated`; both are called only by SECURITY DEFINER functions, which run as owner and keep EXECUTE, so no aggregate breaks. **`_schedule_copy_plan` is left reachable on purpose** — `schedule_copy_month` is SECURITY **INVOKER**, so revoking its helper would break Employee Schedule month duplication for every non-superuser; it carries no pay and returns only rows the caller's own RLS already admits. Verified after the fix, as the same store user: the two helpers deny, `tech_store_month`/`_daily`/`_range`, `dashboard_range_metrics` and `payroll_to_sales_range` all still work, and a schedule duplication preview still returns a plan.

34. `pgw_report_builder_35.sql` — **Report Builder.** Four functions, no tables. **`report_measure_catalog()`** is the one list of what a report may ask for; the ~42 summary/gross-profit/technician/tender measures are static, but the **category measures are built from `brand_service_categories`**, so adding a category to a brand puts it in the picker with no code change (106 measures today: 32 categories x units and % of cars, plus 42 static). The picker AND `report_build`'s validation both read it, so they cannot disagree about what exists. **`_report_tech_daily`** is the day-grain twin of `_tech_pay_range`: guarantee pay and commission are **exact per day** (hours x rate, flag x rate), while **overtime, other pay and total pay are ATTRIBUTED** to days by the day's share of the week's hours, because all three are properties of a WHOLE week — `greatest(guarantee + OT, commission) + other` is decided once over the week and cannot be recomputed per day. Summed over any whole week it is exact, and over a range it equals `_tech_pay_range` to the cent (verified: labour cost $15,640.19 on Millwood July, identical to the dashboard and `tech_store_range`). **`_report_grain`** is the (location, date) universe any of the three sources has data for; it is used to size the answer and to build it, so both agree. Both helpers are revoked from **`public, anon, authenticated`** — the migration-34 lesson, since `revoke ... from public` alone is a no-op in Supabase. **`report_build`** validates, then refuses restricted measures with **42501 before reading a row**, then measures the result and refuses an oversized one with **54000 naming the count and the limit** rather than timing out, then aggregates. **Scope is `can_access_location()` and only that**: `p_locations` NARROWS the set the caller can already see and can never widen it — verified live as a store user, where a crafted request naming another store returned zero rows and `[own, other]` returned only own. **Totals come from `GROUPING SETS` over the same facts**, never summed down the column: on a June–August range the total capture rate is 0.4787, recomputed over the whole set, where the average of the month rows would have been 0.6437. Buckets with no data still appear for store/district/region groupings, because "this store reported nothing" is an answer a district comparison has to be able to give; period groupings emit only periods that have data. **What is restricted, and what deliberately is not:** guarantee pay, commission, overtime and other pay are admin/master; **`tech_labor_cost` is open to everyone**, because Part 2 lists store-level aggregate labour cost as available and "total pay" as restricted, and at store level those are the same number — publishing it twice under two labels is the failure migration 33 refused for Sales. There is no `tech_total_pay` measure; asking for it returns "unknown measure", not a second answer. No grouping here is per-technician, so an individual's pay cannot be isolated even by an admin. **Cash drawer tenders** (cash, checks, cards, Bread, Synchrony, American First, Koalifi, Snap, fleet) are exposed in their own group because Part 2 asks for "every tender type" and the tic sheet has no tender columns — they live on `cash_drawer_closeouts`; fleet is summed out of its jsonb array with a malformed amount contributing zero rather than failing the report. **`jsonb_build_object` has seven measures of headroom** before it hits Postgres's 100-argument call limit; the note in the file says where to split it.

35. `pgw_report_presets_36.sql` — **the five leadership reports.** Task 10 revised: the presets stopped being guesses, so the builder grew the three things the real reports need. **`prior_year_actuals`** (per store per month: sales, gross profit, cars) plus **`prior_year_actuals_import(jsonb)`**, which takes what BDC will actually send — a spreadsheet keyed by STORE NUMBER — resolves the numbers itself, **names the ones it does not recognise instead of failing the file**, and treats a blank cell as "not supplied" so a sales-only file does not erase cars. It is **deliberately NOT backfilled** from `bonus_monthly_targets.last_year_gp`, though nine stores' 2025 GP sits there: Model B's bonus math reads that column, and copying it would put one fact in two tables with no owner. **`report_format_rules`** makes conditional formatting DATA — a cut point in a CASE is a deploy, a cut point in a row is an update — and its `label` REPLACES the value, which is how `BOOM!` appears where a tier is cleared. **`report_pace()`** puts `MTD / days_elapsed * days_open` in SQL for the first time; it existed in three JavaScript copies and none was reachable from a query that aggregates across stores. **`report_build()` was DROPPED and recreated**, not overloaded — every new argument has a default, so a 7-argument call would have been ambiguous between two signatures and PostgREST would have picked one at random. It gains a **SORT** (refused if the measure is not one of the report's own columns, because a silent no-op sort is worse than an error), a **SECOND DATE WINDOW** (`p_alt_measures` names exactly which columns read it, so nothing about a column's window is implicit — the Market Review is month-to-date with one *yesterday* column, the tyre report is yesterday with one *month-to-date* column), and **29 new measures** (135 total). **PROJECTIONS ARE COMPUTED PER STORE THEN SUMMED**: a market's projected GP is the sum of six stores' projections, never the market's MTD over the market's elapsed days times six stores' planned days, which would overstate it roughly six-fold. **The PGW row needs no special case** — per-store measures divide by the store count at whatever level they are computed, so the total row is a company average while projected GP simply sums, which is exactly the mix the sample shows. **THREE BUGS FOUND IN VERIFICATION AND FIXED HERE.** (1) `derived_days_open` (migration 20) referenced `holidays` **unqualified** with no pinned `search_path`, so it worked only because every caller so far reached it through PostgREST with `public` on the path; the first `security definer set search_path = ''` caller failed outright with `relation "holidays" does not exist`. Both it and `annual_days_open` are now qualified and pinned. (2) Buckets were built from FACTS, so a store that had entered nothing lost not just its (correctly empty) sales but its GP budget, its tier thresholds and its planned days — with one store currently carrying data the Month Total GP report would have rendered **35 of 36 rows completely blank**, which is precisely the report someone needs when a store has not reported. The row universe is now every bucket that has SCALARS unioned with every bucket that has facts. (3) The format-rule seed used `on conflict do nothing` with **nothing to conflict with**, so three migration runs left 39 rows instead of 13; a `nulls not distinct` unique index now makes it genuinely idempotent (the default NULLS DISTINCT would not have caught it, since `threshold_b` is null on every rule but a `between`). **Verified live**: market projections reconcile to the PGW total with a gap of **exactly 0**; the total capture rate is recomputed over the whole set, not averaged; `BOOM!` fires on a cleared tier and never on an absent one; a non-admin gets 42501 for the pay breakdown **including when it is smuggled in through the new sort or alt-window parameters**, and 0 rows for any store outside scope **including through the alt window**.
36. `pgw_report_cars_vs_py_fix_37.sql` — **a market that traded nothing read as "no change" against a real prior year.** Found within minutes of loading the 2025 actuals, by running the Market Review for a month only one store had traded: `Charleston — cars/store 0, 2025 cars 6,392, vs 2025 = 0`. Ten stores that reported nothing, against 6,392 real prior-year cars, rendered as parity. `cars_per_store_vs_py` prorates the prior year to the same share of the month the current period covers — `(ro/stores) - ((py_cars/stores) * (traded_days / avg_days_open))` — and when nothing has traded `traded_days` is 0, so the prior-year term is multiplied by zero and the whole expression collapses to `0 - 0 = 0`. The proration did exactly what it was written to do; the mistake was letting a factor of zero stand in for "there is nothing to compare". **This is the third face of one rule.** Task 10 says a missing prior year must not render as zero and must not render as a 100% improvement over nothing; this is the mirror — a missing CURRENT period must not render as no change against a prior year that really exists. Zero is a claim. **Only this measure was affected**: `sales_vs_py` and `sales_vs_py_pct` subtract from `projected_sales`, which is NULL when a store has no traded days, and NULL minus anything is NULL; `cars_per_store_vs_py` builds off `a_ro`, a SUM, which is 0 rather than null. The guard moves into **`report_cars_vs_prior_year()`** so the four cases that have no answer — no prior year, no stores, no planned days, no days traded — are one readable function rather than a condition buried four hundred lines into a query. `report_build` is reproduced whole because Postgres cannot patch one expression inside a function; it was **extracted programmatically from migration 36 with only that expression substituted**, so no line was retyped, and a line-by-line diff confirms the sole change. Verified after: the four untraded markets read blank, Columbia East (which did trade) keeps its figure, and the helper returns null directly for the nothing-traded shape.

37. `pgw_horizon_slots_38.sql` — **Horizon slot persistence, sandbox isolation, and two Florida stores.** Horizon TMG addresses exactly **20 technician slots per shop** and the upload writes into those numbered slots, so if the portal invents its own numbering the first upload overwrites technician history already sitting in Horizon for every store. Two operations rules, both non-negotiable: **never-used slots are consumed before freed slots are reused** (a store with 15 techs where tech 2 was terminated puts the next hire in slot 16, because the longer a terminated tech's slot goes unreused the longer his Horizon history stays attributable), and **a rehired technician takes the next slot in the queue** rather than reclaiming his old one. Both collapse into one `ORDER BY` inside `next_horizon_slot()`: `ever_used asc, last_released_at asc nulls first, slot_number asc`. **`location_horizon_slots` IS NOT `tech_slots`** — three tables, three cardinalities, three masters: `employees` (unbounded, PGW, the roster), `tech_slots` (9, the Excel sheet, a freely reassignable entry grid), `location_horizon_slots` (20, Horizon TMG, an external address space with an allocation discipline). Millwood runs 15 technicians against 9 `tech_slots` rows, which is the proof `tech_slots` is a grid and not a roster; migration 29's `tech_reassign_slot()` permits free movement, correct for a grid and forbidden here. **The two must never be joined** — both relate to `employees`, and the upload resolves `employee_id → Horizon slot`; a `comment on table` on each says so. Slot state persists independently of technician records because a slot that was used and freed has different queue priority than one never touched, and with no technician row pointing at it there is nowhere else to keep that distinction; `ever_used` is a high-water mark, never reset. A trigger provisions all 20 rows per location at creation and the migration backfills existing stores. **The full-store case fails loudly** (`P0001`): twenty active technicians with a twenty-first hired is real at larger stores, and returning null, silently reusing a slot, or defaulting to slot 1 all cause the exact data loss this exists to prevent. `seed_horizon_slot()` is a separate path from `assign_horizon_slot()` because importing Millwood's live assignments must set `ever_used = true` even for a **vacated** slot, which the queue can never produce. **Release is deliberately NOT wired to `employees.active`** — the directions are not symmetric: a terminated technician holding a slot is the safe state, while releasing is destructive and, under the rehire rule, effectively irreversible (flipping `active` back does not restore the slot). `employees.active` exists for payroll filtering, and coupling it here would let a payroll correction silently reorder the queue. The compensating control is the `horizon_slots_held_by_inactive` view behind a standing Tech Tracker notice. **Sandbox is not divested** and must never share a column: divested history is real and must still appear in prior-period reporting, sandbox data is fake and must never appear anywhere. No divested mechanism exists — Rabon Rd, Broad River and Old Bush were never seeded as locations, so that rule is forward-looking and deliberately unimplemented (and **`Midas Bush River` #3936 is a LIVE store**, not "Old Bush" — never flag divestiture by name match). **Part 5 needs two mechanisms, not one.** Visibility goes in `can_access_location()`, whose blast radius is accepted because it backs almost every policy in the portal and one clause hides the sandbox from store/district/regional uniformly. Rollup exclusion cannot go there, because the helper returns true for admin/master by design — so `and l.is_sandbox = false` is added at every cross-location scope site. **There are THIRTEEN of those across seven functions, not the nine a `from/join public.locations` grep finds**: `payroll_to_sales_range` alone has six, because beyond `store_count`, the wages scope and the gross denominator, three more compute the `hours_thru`/`sales_thru` watermarks by joining `payroll_daily`, `tech_daily` and `daily_kpi` to `locations`, and sandbox activity would have dragged those watermarks forward. Grep `can_access_location(l.id)` instead — every cross-store scan uses that exact idiom. Authorization guards of the form `can_access_location(loc)` are left alone: they authorize an explicitly named location, and an admin naming Value Service deliberately is allowed to. All seven bodies were **extracted programmatically from their live sources with only the predicate inserted and round-trip verified byte-for-byte**, so no line was retyped; `report_build` comes from **migration 37**, since 35 and 36 each define an older body and resurrecting one would silently revert the cars-vs-prior-year fix. **The upload guard returns a verdict rather than raising.** Part 4 wants both an abort before the network call and a log of every attempt including refusals, and Postgres can give only one: with no autonomous transactions, an exception rolls back the very audit row worth keeping. So `horizon_upload_target()` returns `(shop_number, authorized, reason)` and **withholds the shop number on refusal** — a caller that ignores `authorized` still has nothing to transmit to, making the stop structural rather than advisory while the log survives. Shop number is read from the location row and is never a parameter (`p_intended_shop` is an assertion input, so a caller that thinks it knows the number fails instead of overriding); null refuses; the pairing is asserted before transmit. Shop number is **data, never derived** — Value Service's is `b306006`, which is not numeric and matches no store number pattern, so any code constructing one from a store number is a bug. Also adds `locations.phone` and a **partial** unique index on `store_number` (partial because sandbox rows have none and a total index would allow only one). Seeds `#2322 Midas Oviedo` and `Value Service` (`is_sandbox`, no district so it cannot roll into Florida — region is derived through the district join, so a null district yields no region with no workaround). Goals are **four months, Sep–Dec 2026 only**, and the loader needed no change: `bonus_monthly_targets` is keyed `(location_id, plan_year, month)` and never required twelve rows. **The trap is the other table** — creating a `store_annual_goals` row makes `v_store_monthly_gp_target` divide by `annual_days_open(2026)` and spread across all twelve months, inventing Jan–Aug targets for a store that was not open; with no annual row, `_report_store_scalars`' `coalesce(b.days_open, v.days_open)` falls through to these four rows and the other eight correctly return null. `bonus_policy` already carried every company-wide scalar in this brief from migration 26, so only the per-store incentive scale is seeded. Verified live: all queue rules exact (16 not 2; ascending 16–20; freed reuse in **release** order 5,9,3 not numeric; a rehired tech landing on 19 rather than his old slot 7; `P0001` at the twenty-first; `23505` on duplicate occupancy), 20 rows for every location with none deviating, both rollups returning 38 of 39 with **no `~unassigned` bucket** for the sandbox, `employees.active` provably not moving slots, and as a `store` user: one location visible, the sandbox invisible even when named by id, `can_access_location(sandbox)` false, and `42501` from assign/release/upload. A twelve-table regression sweep confirmed widening `can_access_location()` broke no existing store access.

38. `pgw_horizon_seed_2320_38a.sql` — **store #2320, `Midas Semoran`.** Split out of migration 38 and held back because the name was contested — the bonus sheet tab read "Semoran", BDC referred to the store as "Orlando" — and every later artefact (handout tabs, bonus CSVs, prior-year imports) matches stores by name or number, so a guessed name is one that fails to match later. Confirmed as `Midas Semoran`; the prefix follows the convention set by rows like `Midas Bush River`. Otherwise identical in shape to #2322 in migration 38: Florida district, $200 float, Model A, 11 cars/day, the standard Model A incentive scale, a null `horizon_shop_number` until credentials are loaded, and **four months of goals only** with no `store_annual_goals` row, for the reason above. Both Florida stores were acquired from a non-Midas operator and are **new to Horizon entirely** — no prior slot history, so their 20 rows provision empty at `ever_used = false` and their first technician takes slot 1 from the normal queue. Neither is part of the Millwood slot collection. Goal figures are seeded as given, never recomputed; the four `days_open` counts (25/27/24/26) were checked against `derived_days_open(2026, m)` and the 308-day basis against `annual_days_open(2026)`.

39. `pgw_horizon_reservations_39.sql` — **reserved slots, and two reversals of migration 38.** *(APPLIED and verified live 2026-09-03.)* The consolidated Brief 38 arrived after migration 38 shipped and carries a concept 38 does not have at all: **a Horizon slot can be reserved for a labor-sales category that is not a technician.** Two kinds, and the reason they cannot be one boolean is a **cost asymmetry**: `front_staff` (every store, exactly one) carries front-of-house labor sales with **no associated labor cost**, so Horizon can account for the sales figure without attributing uncosted hours to anyone; `gs_techs` (SpeeDee only, slot 5) is pooled general-service work and **does carry labor cost**. One flag would either write cost against a slot that has none or drop it from a slot that has some — a silent wrong number in an external system, which is the failure this whole area exists to prevent. `reserve_horizon_slot()` refuses `gs_techs` at a Midas store for that reason, as a function check rather than a constraint so extending the pooled model stays a one-line change. A store may hold one of each; `location_horizon_slots_one_per_kind` is partial on `(location_id, reservation_kind)`, so it enforces one **per kind**, not one per store. **Reservations are not releases** — `clear_horizon_reservation()` deliberately touches neither `last_released_at` nor `ever_used`, because no technician ever held the slot, so no history is at risk and no queue position has been earned; a never-used slot reserved and then cleared returns to the never-used group intact. **Reversal one: `nulls first` → `nulls last`.** Migration 38 read a null `last_released_at` as "vacated long ago, safest to reuse". It means the opposite: **the vacate date could not be reconstructed** — 94 stale slots plus every placeholder and blank below slot 12 are in exactly that state — so we do not know how much technician history is sitting there. `nulls first` handed those out **first**, treating unknown risk as no risk. They now sort last, behind every slot whose history depth can actually be reasoned about; the never-used group is unaffected either way because `ever_used` sorts before the date is consulted. **Reversal two: `location_horizon_slots_occupant_key` is dropped.** Migration 38 added that one-employee-per-slot unique index by analogy with migration 29's `tech_slots` guard; the brief says explicitly not to have it yet, because store 3831 has Carlos Moran in slots 8 **and** 12 in Horizon today and that has to be recorded as-is while it is investigated. The index would reject the seed, and rejecting it does not fix 3831 — it just makes the portal's picture of 3831 silently differ from Horizon's. **The readable guard inside `assign_horizon_slot()` is kept**, so no *new* duplicate can be created through the normal path; only the admin-only seeding path can produce one. Re-add the index once 3831 is resolved. Also: the full-store exception now counts **occupied and reserved separately** (18 occupied plus 2 reserved is a real full store, and a manager told "all 20 occupied" who can count 18 technicians has been handed a message that contradicts what he can see, with a different remedy — release someone, or clear a reservation); `seed_horizon_slot()` clears reservation state, since importing an occupant over a stale reservation would otherwise trip `lhs_reserved_unoccupied`; and **`horizon_upload_target()` gains upload precondition 3 and a `front_staff_slot` output**. Uploading with no Front Staff slot sends front-of-house labor sales either nowhere or into a technician's slot — a silent wrong number rather than a visible failure. Returning the slot number from the authorised verdict is the §8 bridge made structural: **the transport never has a reason to look one up, and therefore never has a reason to hardcode one.** That matters because Front Staff sits at slot 20 at nineteen stores, slot 6 at eleven, slot 2 at three, and slots 5, 7, 9 and 12 at one each — any constant is wrong at most stores, and wrong quietly. On refusal the slot comes back null alongside the shop number, keeping the rule that a refused verdict contains nothing usable.

40. `pgw_horizon_reservations_seed_39a.sql` — **38 shop numbers, 42 reservations, and the 464-row slot import.** *(APPLIED 2026-09-03; occupant resolution is PENDING THE ROSTER — see 41.)* Transcribes `PGW_Horizon_Slot_Reconciliation.xlsx` tab `Slots` into `public.horizon_slot_import`, a permanent table rather than a temp one because the import leaves a **worklist** — 44 spellings to confirm, 94 vacate dates to collect, 3831's duplicate to resolve — that has to outlive the session that ran it. **It writes tables directly instead of calling `seed_horizon_slot()` and `reserve_horizon_slot()`**, because both gate on `current_user_role()`, which reads `profiles` by `auth.uid()`, which is null in the SQL Editor: every call would raise and nothing would import. The statements reproduce those functions field for field, and the invariants are not weakened — the two check constraints and the one-per-kind index do not consult a role. **Store 2320 contradicts migration 38a**, which recorded it as "new to Horizon entirely" with a null shop number: it is shop **142282** and its Horizon draft already carries three working technicians and one departure. 2322 is in the same position (**143284**). Left as 38a described them, their first upload would have written new slot-1 data over an existing technician's history. **2321 is the one shop number in no workbook tab** — the reconciliation's own Issues tab flags it outstanding — and comes from the brief (**122288**); the partial unique index means a wrong value surfaces as a collision rather than as a silent overwrite. **Automated name matching is not seeded from.** 164 slots carry an occupant; 120 matched cleanly and 44 could only be matched by guessing at a spelling — nicknames sharing no letters with the payroll name (Cash/Carter, Ty/Tyrell, Vick/Demetrius, Spenze/Timothy), three roster entries with the order reversed, and misspellings (Olvier/Oliver, Rainer/Rainier, Pulintano/Pulitano). Getting one wrong produces no error; it points the upload at the wrong man's slot. So **the 44 are not assigned**, and matching is done on the **roster** spelling against `employees.full_name` — never on the Horizon spelling — through an order- and punctuation-insensitive token-set key. **An unresolved occupant imports as freed with no date, not as empty**: those slots really are occupied in Horizon, and `ever_used = true` with a null date puts them at the very back of the queue under migration 39's `nulls last`, reachable only once the store is otherwise exhausted, and reported in the meantime. The alternative would hand an occupied slot to the next new hire. **No `employees` rows are created**, no vacate dates are invented, and no Horizon credentials are seeded (every workbook tab carries a plaintext password beside the shop number, which must not land in a column RLS exposes to a store role). **Slots 13–20 at the 12-slot stores are absent from the workbook and stay that way** — the Excel process could not physically address them, so migration 38's provisioning already has them right at `ever_used = false`; only 3935 runs a 20-slot template. **Existing reservations do not move**: nineteen stores keep theirs where Horizon has it and nineteen gain one at slot 20, because relocating a reservation would strand its accumulated labor sales in the old slot. Only the label changes — every source wording (MN & SA, MN SA, Manager or SA, MANAGER, Management, midas store, Frontshop, front staff) normalises to `Front Staff`. Exceptions handled explicitly: **3935** keeps slot 12 and releases slot 6 as an ordinary freed slot; **3136** keeps William Sigmon at slot 1 and releases slot 6; **Michelin** at 2321, 3923 and 3009 slot 12 is freed dated `2023-06-01`, so it sits ahead of the undated freed slots and behind every never-used one; **SpeeDee** keeps the pooled model — 8 stale slots freed, the 2 active named technicians retained, and **no slots assigned to the 21 unplaced roster technicians, which is the model working, not a gap**. **The 3831 call is a decision, not a transcription**: the workbook classes slot 12 STALE with an empty roster match, but that is a matcher artefact — it had spent Carlos on slot 8 and could not match him twice. The brief says "seed both as-is and flag" and confirms the reading by noting the result violates one-employee-per-slot, which only genuine double occupancy can do; the contrast with 3136, where the brief asks plainly for a release, settles it. Recording reality is the safer error in either direction. **Verified before shipping** by replaying migrations 01→39a into a real Postgres engine (PGlite) and running the brief's own §10 checks against the result: 39 shop numbers all distinct, 38 Front Staff reservations at exactly the brief's slots with a single label, every workbook reserved slot matching where it was applied, 3935 correct on both slots, the four SpeeDee stores holding both kinds, `ever_used` in slots 13–20 zero everywhere except 3935, Michelin dated at all three stores, and `location_horizon_slots_occupant_key` gone. Twenty-three behavioural checks pass against the same engine, including the two reversals: freed reuse in **release** order rather than slot order, an undated freed slot sorting **last** rather than first, a reserved slot never returned by the queue, a second `front_staff` rejected, `gs_techs` refused at a Midas store, clearing a reservation leaving `ever_used` false and `last_released_at` null, the full-store message naming occupied and reserved separately, and `horizon_upload_target()` refusing with nothing usable then returning the reserved slot number — 14, not a constant — once a Front Staff slot exists.

41. `pgw_horizon_roster_prep_39b.sql` — **fictional employees removed, two departures retired, slot assignment frozen.** *(APPLIED 2026-09-03. One defect found afterwards by store-role verification — see 42.)* Migrations 39 and 39a applied cleanly and verified — 28 of 33 live checks passed and **all five failures traced to one cause: `employees` held 9 rows across 3 stores rather than the 185-technician roster**, so 162 of 164 Horizon occupants had nothing to match and imported as freed-with-no-date. That is the specified behaviour, not a defect; this file clears the three things that would be wrong to carry into the roster load. **Three fictional employees are DELETED** — Tom Cruise, Dwayne Jonhson (the typo is in the data; matching "Johnson" would silently delete nothing) and Albert Einstein, all at #5254 from a July UI test. A slot occupant is an upload payload field, so once the roster lands and 39a re-runs, nothing structural stops a fictional name being written into the franchisor's coaching system; only one of the three is `position = 'tech'` today, but position is editable and all three are equally fictional. **Every dependent table is checked first and a row with any history is skipped with a warning** — three of the eight references are `ON DELETE SET NULL` or `CASCADE`, so a careless delete would destroy attribution rather than fail. **Alan Barron and Joseph Fabre are marked INACTIVE, not deleted**, which resolves the disagreement 39a surfaced: the workbook classed Millwood slots 2 and 7 STALE while the portal still had both active, and **the workbook was right**. They keep their rows because they carry 3 and 8 `tech_daily` rows on Millwood's July sheet — the only genuine operational data in the portal — and `tech_daily.employee_id` is `ON DELETE SET NULL`, so deleting them would silently un-attribute costed technician days and reintroduce exactly the unattributed-days problem migration 29 exists to prevent. **This changes no slot state, by design**: migration 38 deliberately does not wire `employees.active` to occupancy, and their slots are already freed-with-no-date, which is the correct resting state for a departure whose date nobody has — under 39's `nulls last` they sit at the very back of the queue. **No vacate date is guessed**, because a wrong date would move them *up* the queue ahead of slots whose dates are known; those belong in the workbook's Vacated tab and load later through `seed_horizon_slot()`. Their `tech_slots` grid rows are left alone — that grid is the store's, freely reassignable, and not a roster. **Slot assignment is FROZEN** via a single-row `horizon_config` table following migration 32's `payroll_config` precedent, read by a guard placed *ahead of the role check* in `assign_horizon_slot()` so a frozen system says "frozen" to everyone instead of telling an admin he lacks permission; the message prints the one-line lift statement. The 162 freed-with-no-date slots are allocatable, and although never-used slots 13–20 are handed out first so real exposure is small, at a store where 13–20 are exhausted the next assignment takes a slot about to be re-seeded as occupied — which under the rehire rule is not cleanly unpickable. **`seed_horizon_slot()`, `release_horizon_slot()` and the reservation functions stay open**: the roster load itself runs through seeding, and a genuine departure during the freeze should still be recordable. ⚠️ **Re-running 39a §3–§4 is a FULL re-import from the workbook** and reverts any live slot change made in the interim — the freeze closes the assignment path but not release, so note and re-apply any release, or lift the freeze and re-run 39a first. Twelve behavioural checks pass against a real Postgres engine with the live shape reproduced: the fictional rows go, Barron and Fabre survive with all 11 `tech_daily` rows still attributed, slots 2 and 7 unmoved by the active flip, assignment raising `P0001` naming the freeze, seeding and release still working, assignment restored the moment the flag is lifted, a "test" row that has acquired a dependent skipped rather than deleted, and a third consecutive run applying clean.

42. `pgw_horizon_freeze_order_39c.sql` — **the freeze check belongs after the role check.** *(APPLIED and verified live as `teststore` 2026-09-03. Corrects 39b.)* Caught by store-role verification, which is the only way it could have been caught: 39b put the slot-assignment freeze at the very top of `assign_horizon_slot()`, ahead of the role check, on the reasoning that a frozen system should say "frozen" to everyone rather than tell an admin he lacks permission. **That reasoning was wrong** — an admin *passes* the role check, so the freeze message reaches him from either position; the ordering only ever changed what NON-admins see, and it changed it twice for the worse. **The denial code moved.** A store user calling `assign_horizon_slot` had returned `42501 'Only an admin can assign a Horizon slot'` since migration 38, whose own verification asserts exactly that; under 39b they got `P0001 '...is frozen...'` instead, so an authorization failure was reported as a transient operational state — and since `release_horizon_slot()`, `reserve_horizon_slot()` and `horizon_upload_target()` all still returned 42501, 39b quietly made assign the odd one out. **And it leaked internal state**: `freeze_reason` names the roster gap and the count of unresolved slots, which is operational detail for an admin, not an answer to a store manager who asked only whether he could assign a technician. Fixed by moving the freeze below the role and location checks — authorization first, so a caller who may not assign at all learns nothing about system state, and only a caller who *would* have been allowed is told why he cannot right now. **The config table's read policy is tightened in the same file**, from 39b's `using (true)` to admin/master, matching `horizon_upload_log`: `freeze_reason` was readable by every signed-in store user through a second route, and closing one without the other would have been theatre. Nothing changes for an admin or master. Verified live as `teststore` before the fix (assign returning P0001 with the reason text) and after applying: store gets 42501 from all four functions with no mention of the freeze, `horizon_config` returns zero rows, master still gets the freeze message with its lift statement, and the 42 reservations are untouched. **Note the harness limit this exposed:** the PGlite dry-run runs as superuser and bypasses RLS entirely, so it passed the function-level 42501 checks and could not see the `horizon_config` policy at all — RLS remains a live-only verification, exactly as the store-role pass for migration 38 was.

43. `pgw_horizon_sandbox_upload_40.sql` — **the sandbox can upload, and only the sandbox.** *(APPLIED and verified live 2026-09-16, as master and as `teststore`.)* First step toward the portal's own Horizon upload, built so it can be tested against Value Service (shop `b306006`) while the stores keep uploading from their workbooks. **Value Service gets its Front Staff reservation at slot 6**, not 20: its first job is the July comparison (Millwood → Value Service), Millwood's Front Staff is slot 6 (the workbook's "MANAGER OR SA" sheet feeds `kpi_tech_6_*`), and the store workbook's macro only sends 12 technician blocks, so 20 might never be sent. Written directly, because `reserve_horizon_slot()` cannot be called from the SQL Editor; an existing Front Staff reservation is left where it is, and an occupied slot 6 stops the migration. **`horizon_config.upload_sandbox_only` (default true) makes `horizon_upload_target()` refuse every non-sandbox location** — preconditions 1–3 stop data reaching the *wrong* shop, but nothing stopped a *correct* portal upload overwriting what a manager already sent from the workbook. The refusal is a logged verdict like the other three, sits after the role check (39c's lesson), is checked first among the preconditions, and **fails closed** — a missing config row reads as sandbox-only. Lift with `update public.horizon_config set upload_sandbox_only = false, updated_at = now() where id;` — **only after the sandbox comparison matches Millwood.** Dry-run 14/14 (including the lift, the missing-row case, a re-run, and the occupied-slot stop). Live: SQL Editor checks — one reservation, slot 6 / front_staff / never used; switch on with its reason; the 42 real reservations unchanged. As master: Value Service authorized with `b306006` and slot 6; Millwood refused with the shop number and slot withheld; both attempts logged. As `teststore`: `42501 Only an admin can upload to Horizon` with no mention of the switch, zero rows from `horizon_config` and `horizon_upload_log`.

44. `pgw_horizon_credentials_41.sql` — **Horizon shop passwords in Vault, released once, only against an authorized upload.** *(APPLIED and verified live 2026-09-16, as master and as `teststore`.)* Horizon's importer takes the shop password in the request body, so the portal needs it — but no signed-in user ever should. **Passwords live in Supabase Vault as `horizon_password:<shop number>`**, added through the Dashboard's Vault screen (never in SQL, a migration, or chat); only Value Service's (`horizon_password:b306006`) is stored. **`horizon_upload_target()` is dropped and recreated to also return `attempt_id`** (a return-type change cannot be made in place; nothing called it yet). **`horizon_release_credentials(attempt_id)` is executable by `service_role` only** — i.e. the future upload Edge Function — and releases a password only for an attempt that is `authorized`, under 2 minutes old and never released before (`horizon_upload_log.credential_released_at`, row-locked), after re-checking live state: the sandbox-only switch, the shop number and sandbox flag against the logged ones, and exactly one Front Staff slot. It raises rather than returning a verdict, because its caller is a program and the attempt is already logged. **`horizon_credential_status()`** lets admin/master see which shops have a password and when, reading the encrypted `vault.secrets` table, never the decrypted view. The file stops with instructions if Vault is not enabled. Dry-run 23/23 (with a Vault stand-in): single release, refused/unknown/stale attempts, the switch re-enabled between approval and release, a changed shop number, a missing secret and a removed Front Staff slot all refuse, and none of the refusals marks the attempt released. Live: SQL Editor — the column exists, `authenticated` and `anon` cannot execute the release function, the secret is present by name. As master: status lists 39 locations, Value Service has a password and no real store does, no password field is returned; the upload check returns attempt #12; calling the release function from the browser gives `42501 permission denied` and #12 stays unreleased. As `teststore`: status, upload check and release all return 42501, and the upload log reads zero rows. **Open:** the macro also sends HTTP basic auth with a placeholder username — ask Horizon whether it matters before storing anything for it.

45. `pgw_sandbox_copy_millwood_july_42.sql` — **Millwood's July 2026 copied into Value Service**, the source for the first sandbox upload. *(APPLIED and verified live 2026-09-16.)* Data only. Copies Millwood's **live** rows at run time, not the 24s/25s seeds (live data has drifted from them before): 5 employees with `employee_pay_rates` and `tech_pay_rates`, all 9 tech grid slots, the 83 July `tech_daily` rows, weekly other pay for the weeks touching July, the 26 July `daily_kpi` days with their 237 service-unit rows, and the 30 category goals plus the tic-sheet goal. **Every column is copied**: a session-only helper reads the column list from the catalog, so a column added later cannot be silently dropped, and only ids and `location_id` change. `tech_daily.employee_id` is mapped explicitly, because pay resolves from it. Payroll, cash drawer, schedules and bonus are not copied — Millwood has no July payroll, and the sandbox has no bonus plan by design. **The sandbox's Horizon slots follow the July workbook's layout, not Millwood's current one** — 1 Brayboy, 2 Barron, 4 Cantrell, 6 Front Staff, 7 Fabre, 8 Jones — because Barron, Fabre and Cantrell all worked in July but no longer hold Millwood slots in the portal, so today's layout would give their July numbers nowhere to go. Slot 12 stays empty: the workbook names Rick Schroeder there, but he has no employee row and no July numbers, so the only expected difference is his name in `kpi_tech_12_name`. **Each run resets Value Service first** (employees, grid, days, goals deleted; Horizon slots back to never-used apart from the Front Staff reservation), all in one DO block; it stops with nothing changed if a payroll row exists there, Front Staff is not slot 6 at both stores, or a layout name matches more than one Millwood employee. Dry-run 30/30: every table identical row for row, no August rows, Millwood's data and slots untouched, no cross-store references, the upload check still approving the sandbox, and a re-run, a hand-added row and each stop condition behaving as described. Live: counts 5/5, 9/9, 83/83, 26/26, 237/237, 30/30; July totals identical at both stores (741.65 hours, 370.33 flag hours, 58,582.50 labor); sandbox slots exactly as above; Millwood's slots still 1 Brayboy, 6 Front Staff, 8 Jones. As `teststore`: one visible location, and employees, grid, July days, tic sheet, Horizon slots and goals all return Millwood's rows only (one Alan Barron, not two). ⚠️ **Open design question for the upload:** `location_horizon_slots` records who holds each slot *now*; uploading a past month needs who held each slot *that month*.

46. **Edge Function `horizon-upload` — PREVIEW ONLY** (`pgw-portal/supabase/functions/horizon-upload/`, no migration). *(DEPLOYED and verified live 2026-09-16.)* Builds exactly what a store workbook's macro POSTs to Horizon, from portal data, and returns it with the password withheld; **it has no send path and never reads the password.** `POST { location_id, month: 'YYYY-MM', mode: 'preview' }` with the signed-in admin's bearer token. The caller's own session runs `horizon_upload_target()` (role, sandbox-only switch, shop, pairing, Front Staff; the attempt is logged under that admin), then the service-role client reads the month through `load.ts` and `payload.ts` builds the fields. **`payload.ts` treats the workbook as the specification:** labor sales = the tech tracker's labor + half the discounts + half the Groupon (Summary G + the uploader's formula), parts = parts + supplies + the same two halves, daily compensation = the tech sheet's P column including its deliberate over-allocation (`techPayMath.js` explains why), one pay rate per person per week, only the month's own days in week 1, and a week with no worked days giving 0, as the uploader's ISERROR does. It computes in JavaScript doubles in the workbook's order and writes numbers as VBA does (15 significant digits), and encodes as `Module1.URLEncode` does. Front Staff days go to the slot `horizon_upload_target()` returns and carry no labor cost; `local_*` categories are never sent; slots 13–20 are sent only when in use. Anyone with days but no Horizon slot is listed as a warning. **Verified against the workbook itself:** `fixtures/millwood_2026-07_workbook.json` is the macro's exact output rebuilt from the Millwood July file's saved cell values (2,800 fields, password replaced by a marker). Value Service's July preview has 2,802 fields, **2,794 identical** — all 2,759 daily values match to the digit — and the rest are decided differences: shop `b306006`; empty tech names sent blank where the workbook sends `0` (tech 3, 5); tech 6 named "Front Staff", not "Manager or SA"; tech 12 blank (Rick Schroeder is not in the portal); and the A/C & Heat and Engine Performance monthly goals added (the macro stops reading goals at a blank row, so it never sent them). Totals: 377 ROs, labor 57,112.33, parts 58,330.54, tires 25,363.72, discounts −1,997.91, labor cost 15,674.27, parts cost 23,977.61, tire cost 17,962.78. Live as master: the deployed function gives the same fields as the local build; Millwood refused by the switch; `mode: 'send'` refused; no token or a forged one 401; bad month or location 400; unknown location 404; GET 405; the preview's attempt stays unreleased. As `teststore`: 403 "Only an admin can upload to Horizon" for its own store and any other. **Deploy:** `npx supabase functions deploy horizon-upload --project-ref ledmjsfjhvlwjyxjhlyi --no-verify-jwt --use-api` — no Docker here, and the project signs sessions with asymmetric keys, so the token is checked inside the function and again by PostgREST. In PowerShell use `npx.cmd`; the execution policy blocks `npx.ps1`. **Next:** the send path (release the password, try no login, then the placeholder username on a 401, and record Horizon's reply), and a screen to run it.

47. `pgw_horizon_upload_results_43.sql` + **the `horizon-upload` send step** — **the upload log records what happened, and the function can send.** *(Migration APPLIED, function DEPLOYED, both verified live 2026-09-16. No real send has been made yet.)* **43** adds `purpose` ('preview'/'send'), `month`, `field_count`, `fields_sha256`, `auth_tier` ('none'/'username'), `response_status`, `response_body` (first 4000 characters) and `completed_at` to `horizon_upload_log`. They are written only by **`horizon_record_result()`**, which is `service_role` only and records once per attempt: a preview can carry no status, login tier or released password, and a Horizon status requires the attempt to have released its password. **`horizon_release_credentials()` is re-created with one more rule: an attempt that already has a recorded purpose releases nothing**, so a preview, or a send that was stopped, can never turn into a send. **The function:** `mode: 'send'` requires `confirm_sha256`, the fingerprint of the approved preview (SHA-256 of the fields with the password still a placeholder). The fields are rebuilt and must match, or the reply is 409 and nothing is sent. Only then is the password released, the released shop, Front Staff slot and location checked against the approved attempt, and the body posted. `transport.ts` posts `application/x-www-form-urlencoded` to `coaching.horizontmg.com/importer/coaching.php`: no login first, then Basic `username:` only after a 401, stopping after a second 401. Redirects are not followed, the timeout is 60 s, and only a 200 counts as success (the macro's test). Horizon's reply is recorded, and the password is never returned or logged. Previews are now recorded too. **Run 43 before deploying this function**, or it cannot record. Dry-run 22/22 on 43; 13/13 fake-Horizon tests (200; 401 then 200; two 401s; 500; a redirect; network errors; the exact request; the fingerprint); `npx deno@2 check index.ts` clean (a `--use-api` deploy does not type-check). Live as master: preview #15 matches the workbook (2,794 of 2,802) and is logged as a preview with its fingerprint; a send without a fingerprint gives 400 and creates no attempt; a send with a wrong fingerprint (#16) gives 409, logged "Not sent", with no status and no password release; Millwood preview and send give 403 from the switch. As `teststore`: preview and send give 403, `horizon_record_result` is permission denied, and the log reads zero rows. **Still to do:** the first real send (Value Service, July 2026, fingerprint `042b37a9…d08a23`) waits on explicit approval; then compare Value Service with Millwood inside Horizon; then a portal screen to run it.

48. **The first sandbox upload succeeded — and what it took.** `pgw_sandbox_shop_number_fix_44.sql`, `pgw_sandbox_shop_number_B306006_45.sql`, and a fix to `horizon-upload`. *(All applied or deployed and verified live 2026-09-16.)* **Horizon answers HTTP 200 even when it refuses an upload**, with a reply beginning `Error:`. The macro treats any 200 as success, and so did `transport.ts` at first, so the first send was wrongly reported as "accepted". **Fixed:** a send now succeeds only on a 200 whose reply does not start with "Error" (4 more fake-Horizon tests, 17/17). The function also trims whitespace around the Vault password, as the macro trims its cell, and reports only *whether* it trimmed. **Value Service's importer shop id is `B306006`, capital B, confirmed with Horizon.** Brief 38's `b306006` was answered with "Error: Password not accepted." (#20), so the store was found and only the old password was wrong. Migration 44 changed it to `6306006`, which was answered with "Error: Store not found in system." (#23). Migration 45 sets `B306006`, and the password is now in Vault as `horizon_password:B306006`. Both migrations change only the sandbox's `horizon_shop_number` (and the switch's reason text), and each stops with nothing changed unless a Vault secret with exactly the matching, case-sensitive name exists; dry-runs 12/12 and 13/13. The unused Vault entries (`b306006`, `b6306006`, `6306006`) were deleted by the user. **⚠ Do not re-run migration 38's seed section: it would set the shop back to `b306006`.** **Result — send #26 (preview #25, approved fingerprint `54b9a6e2…8786f1`):** HTTP 200, `26 day(s) of data imported successfully. 1 monthly data imported.`, no HTTP login needed, password released once. 31 days were sent and 26 imported, matching Millwood's 26 trading days, so Horizon evidently skips days with no ROs. Nothing from #20 or #23 was imported. **Horizon hides technician names in its UI**, so the Value Service ↔ Millwood comparison in Horizon has to be done on the numbers. **Still open:** that comparison; a portal screen to run preview and send; whether to lift `upload_sandbox_only`; how a past month finds who held each slot that month; and each real store's importer id and password.

49. `pgw_horizon_store_uploads_46.sql` + **"Send to Horizon" on the Daily Tic Sheet — store managers send their own numbers.** *(Migration APPLIED, function DEPLOYED, screen verified in the preview 2026-09-16 as `teststore` and as master. Real stores still cannot send: `upload_sandbox_only` is on.)* Requirement: store managers close out each working day by sending the month so far, as the workbook macro does; this is not admin-only work. **46:** `horizon_upload_target(p_location_id, p_intended_shop, p_purpose)` is dropped and recreated. It accepts role `store` for the user's own store only (via `can_access_location`, which already refuses the sandbox); district and regional are **not** included yet, because no account exists to verify them. It takes `p_purpose` ('preview' | 'send'): **a preview is no longer blocked by the sandbox-only switch; a send still is.** It stores `requested_purpose` on the log row and returns `caller_role`. A store user is given a plain reason, while the full reason (shop numbers, ids, the lift statement) is still logged. `horizon_release_credentials()` now also requires `requested_purpose = 'send'`, so a preview attempt can never release a password, even before its result is recorded. New `horizon_last_upload(location)` returns the latest send and whether Horizon accepted it (a reply starting "Error" reads as not accepted); it is readable by store users for their own store. Dry-run 29/29. **Function:** passes the purpose; a store manager may use only the current or previous month (the end-of-day send, plus the first working day of a month), and nobody a future month. **Per-technician pay is hidden from store users**: every `kpi_tech_N_daily_compensation` comes back as "(hidden)" and the slot summary has no pay, while the store's total labor cost stays, as elsewhere in the portal. The fingerprint still covers the real values. The function also returns a per-slot summary read back from the fields, and `send_enabled` (sandbox, or the switch lifted). Store-facing messages are plain English. **Screen:** `hooks/useHorizonUpload.js`, `components/HorizonSendModal.jsx`, and a button plus a "last sent" line in `TicSheetView`. Pressing the button reviews the page's month (days covered, eight totals, technicians by Horizon slot, warnings); Send carries the reviewed fingerprint and shows Horizon's answer. A ref guard stops React's development double-effect from logging two previews per open. Also fixed the Tic Sheet subtitle showing "#null" for a store with no number. **Verified live — `teststore` (#3303):** button and "Not sent from the portal yet." shown; the September review (1–16) has totals and slots 1 Brayboy / 6 Front Staff / 8 Jones with no pay column, all 372 pay fields "(hidden)", and Send disabled with the plain message; August allowed; July refused (older than last month); October refused; another store refused; a forced send refused plainly; `horizon_config` and the log invisible; the phone-width layout usable; every request on the page 200. **Master:** Value Service July matches the workbook (2,794), fingerprint `54b9a6e2…` unchanged, pay shown, Send enabled, and "last sent … accepted: 26 day(s) of data imported successfully"; Millwood July reviews with pay but Send is disabled, and a forced send is refused by the switch; an admin may review 2025-01. The review was cancelled; nothing was sent. **Millwood's July review shows the open past-month problem live:** Barron (3 days), Fabre (8) and Cantrell (23) hold no Horizon slot today, so their hours and pay would not be sent under any slot.

50. `pgw_horizon_manager_uploads_47.sql` — **district and regional managers send too.** *(Migration APPLIED, function DEPLOYED, district verified live 2026-09-16 as `dm@pgwsupport.com`, a District Manager for Columbia East. **Regional verified live 2026-09-17** as `rm@pgwsupport.com` (Columbia region): review refused for the sandbox (403), built for #3229 Harbison with individual pay hidden. No regional send was attempted — real stores are still behind the sandbox-only switch.)* Decided by the user after 46 left these roles out for want of an account. 47 is create-or-replace of 46's `horizon_upload_target()` and `horizon_last_upload()` with the role lists widened to `district` and `regional`. **Every non-admin role is checked by `can_access_location()`**: a district manager reaches the stores in their district, a regional manager the stores in their region, and neither ever reaches the sandbox. The out-of-scope message is now role-neutral ("You can only send numbers for stores you manage."), and every non-admin role gets the plain refusal text. In the function, **every non-admin role is a "manager"**: this month or last month only, and **no per-technician pay** (the portal keeps the pay breakdown to admin and master, migration 35). `HORIZON_UPLOAD_ROLES` now includes both roles, so the Tic Sheet button shows for them. Dry-run 18/18: a district manager reaches its own district's stores but not another district or the sandbox; a regional manager reaches its region but not another region or the sandbox; a store manager is still limited to its own store; a district preview never releases; and after a lift a district send releases only that store's password. **Live, as the district manager:** sees exactly the 6 Columbia East stores. Millwood (September 16 days, August 31) and Pleasantburg (September, shop 129288) review with every pay field "(hidden)", no pay in the slot summary, and Send disabled. July is refused (older than last month). Harbison (another district), Semoran (another region) and the sandbox are refused ("stores you manage"). A send carrying Millwood's real fingerprint is refused by the switch with the plain message, and nothing is sent. `horizon_last_upload` works for Millwood and is refused for Harbison. `horizon_config`, `horizon_upload_log` and `tech_pay_rates` read zero rows. The Pleasantburg review screen shows no pay column and the disabled-Send note. As master afterwards, the sandbox July fingerprint is unchanged (`54b9a6e2…`) and a Millwood send is still refused.

51. `pgw_adjustments_48.sql` — **Groupon becomes Adjustments; district manager+ edit; re-send flag.** *(Migration APPLIED, `horizon-upload` REDEPLOYED, verified live 2026-09-17 as master, `teststore` and `dm@`. Regional verified live 2026-09-17 as `rm@pgwsupport.com`, Columbia region.)* **Rename:** `daily_kpi.sales_groupon` → `sales_adjustments`, values untouched, label global (no per-store config). **Decided by the user and deliberately NOT renamed:** the Report Builder measure key `sales_groupon` and `dashboard_range_metrics`'s `groupon` output — only labels changed ("Adjustments", "Sales (excl. Adjustments)", "Gross Sales/Profit/Profit % (incl. Adjustments)"). The three functions that read the column (`dashboard_range_metrics`, `report_measure_catalog`, `report_build`) are recreated from 36/38 with only `k.sales_groupon` and the labels changed. The Excel export sizes columns from the label, so four columns widen slightly (24→28, 30→34, 31→34, 33→34) — **kept, decided by the user.** **Horizon is unchanged:** Adjustments were never their own Horizon field; half goes into `kpi_sales_labor`, half into `kpi_sales_parts` (the workbook uploader's formula), and a post-deploy preview of Value Service July reproduced the accepted send's fingerprint `28abc67a…` exactly. **The function must be redeployed right after the migration** — the old build selects `sales_groupon`. **Every formula is unchanged:** Sales excludes Adjustments; the modal's GP adds them; the bonus GP splits them 50/50. Value Service 7/1: Sales $8,204.33, GP $6,831.96 (Adjustments 810.53 — entered live; the seed has 0.00). **Permission:** `daily_kpi_adjustments_guard` (BEFORE INSERT OR UPDATE, SECURITY DEFINER) refuses a non-zero insert or a change unless the caller is district/regional/admin/master AND `can_access_location()`; an unchanged value passes, so store users still save the modal (the UI also stops sending the locked field). **Writes with no signed-in user (SQL Editor, service_role, seed/copy scripts) are allowed — decided by the user.** It stamps `adjustments_updated_by/_at`; callers cannot forge them. District/regional never reach the sandbox, so only admin/master can set Value Service's. **Re-send flag:** nothing held per store-month state, so `horizon_month_status` (location_id, month) is new, SELECT by `can_access_location`, no write policy. The guard raises `needs_resend` when Adjustments change on a month with an ACCEPTED send (keeps the earliest `needs_resend_since`, takes the latest editor, records `needs_resend_last_at` in clock time). `horizon_upload_log_clear_resend` (AFTER UPDATE on the log) clears it when a send is recorded as accepted **and the last change predates that send's `attempted_at`** — the payload is built after the attempt opens, so an older change is in what Horizon got; a newer one may not be. Failed or "Error:" sends never clear it. "Accepted" is now one function, `horizon_send_accepted()`, also used by `horizon_last_upload()`. `horizon_month_resend_status(loc, month)` feeds the UI: yellow warning banner under "Last sent" (who/when), a warning-yellow **Re-send needed** button, and a dot on each day changed since the last accepted send. **Verified live:** master unchanged save → no flag; 810.53→810.54→810.53 → flag up, earliest time kept; accepted sandbox re-send (attempt #59, same fingerprint) → cleared. `teststore`: locked field + tooltip, modal save succeeds, direct change 42501, stamp forgery ignored, sandbox/other store's flag 42501, flag table rows hidden. `dm@`: edited Millwood 7/2 (0 → −1.25 → 0, stamped, no flag since Millwood has no accepted send), sandbox write refused by the trigger, field editable in the modal. `rm@`: sees the 12 Columbia stores only, edited Millwood 7/6 (0 → 2.50 → 0, stamped, no flag), refused on the sandbox and on #2320 Semoran — a real store outside its region (insert refused by the trigger, update matches 0 rows, flag 42501). Columbia West has no tic-sheet rows yet, so the in-region edit ran on Millwood rather than creating a row on a real store. **Also fixed:** the breakdown modal used to close silently on a failed save; it now shows the error and stays open, and `saveSummary` reads the row back so an RLS-filtered update (HTTP 200, no row) is reported as a failure.

52. `pgw_dashboard_gp_adjustments_49.sql` — **the two gross profits become one.** *(Migration APPLIED and verified live 2026-09-17.)* The portal published two gross profits differing by exactly the Adjustments amount (see the resolved note under Known gaps). The user decided the figure INCLUDING Adjustments is correct, so `dashboard_range_metrics` is create-or-replaced — same signature, same columns — with `gross_profit` = Sales + Adjustments − (labour + parts + tyre cost) and `gross_profit_pct` over Sales + Adjustments. **Only those two expressions changed**; the body is migration 48's otherwise. `gross_sales` deliberately still holds the tic sheet's Sales with Adjustments EXCLUDED, so the Sales widget and `payroll_to_sales_range` keep their one denominator — the dashboard's Sales tile and its GP tile therefore sit on different bases on purpose, and the GP tile now reads "of sales + adjustments" and lists the Adjustments it includes (`RangeWidgets.jsx`, which reads the `groupon` column — name kept in 48). **The percentage moved too, deliberately:** changing the numerator alone would have produced a third GP% matching neither screen. Dry-run: dashboard GP and GP% equal `report_build()`'s to the cent for Millwood July, Value Service July, a part-month range and all stores; every other dashboard column byte-identical to 48.

53. `pgw_bonus_flags_resolved_50.sql` — **two Model B questions answered.** *(Migration APPLIED and verified live 2026-09-17.)* The user confirmed both: **the STORE MANAGER receives Model B's 6% of GP improvement over last year** (the handout reads "***PLUS 6% OF ALL GP IMPROVEMENT OVER LAST YEAR***" and names nobody; migration 26 assumed the store manager and flagged the guess — the assumption was right, so no payout changed), and **"grow GP by 10.01%" means clearing the seeded "+10.01% LY" threshold**, which the handout floors at $35,000, with the doubled credit-app and tire bonuses riding the same rung. **Resolved, not deleted:** `bonus_flags` gains `resolved_at` + `resolved_note`, because migration 26 re-seeds every flag with `on conflict (code) do update` and a deleted row would reappear on its next run — the upsert does not touch the two new columns, which the dry-run proves. `useBonusTracker.js` now reads `resolved_at is null`, so answered questions stay in the table as the record of the decision and leave the screen. **Three places claimed the assumption and all three were corrected** — the flags panel, the bonus line (now "GP improvement over last year (6%) — store manager", and its note no longer says "recipient unconfirmed") and a standing-decision footnote in `BonusView.jsx`. Verified live on #3984 Decker as `rm@`: its panel lists 6 open questions and neither Model B question is among them (9 flags live: 2 resolved, 7 open; the panel is scoped, so a store sees fewer), the two rows carry their answers in the database, the other 7 flags are untouched and `model_b_improvement_pct` is still 0.06. Dry-run 7/7, `bonusMath.test.mjs` 113/113.

54. `pgw_wesmark_tire_overage_51.sql` — **Wesmark's tire overage anchors at 11, and three more questions answered.** *(Migration APPLIED and verified live 2026-09-17 as master.)* The user gave Wesmark's (#3938 Midas Sumter) real overage line — **"Each unit per day above 11 will add an additional $500 to the pool"** — so the increment moves off its BOTTOM row, where migration 26 seeded it because the handout read "above 8" against a 8/9/10/11 ladder. **THIS CHANGES MONEY at Wesmark and nowhere else:** at 12 tires/day the pool was 2,500 + 500 x floor(12-8) = 4,500 and is now 2,500 + 500 = 3,000; at 11.9/day it was 4,000 and is now 2,500. The old rule paid the increment again for tires the 9/10/11 rungs already covered. No Wesmark tire data exists, so no figure anyone had seen moved. The update is written as "clear every increment on this store's tire ladder, then set it on the highest threshold", so it is state-independent and re-runnable. **Wesmark stops being the exception** `bonus_incentive_tiers`, `lib/bonusMath.js` and `bonusMath.test.mjs` all had to describe — the test fixture now carries the real ladder plus cases at 8, 9.2, 11.9 and 12 (115 assertions). Also answered, all confirming what was already seeded so nothing recalculates: **the credit-app kicker uses the HANDOUT amounts** ($500 at 50 apps, $1,500 at 100, minimum Silver GP — Millwood's workbook was stale), **SpeeDee Lexington (#3308) is Model D** (BDC's earlier word of Model C is superseded), and **the unusual tire scales at Lake Murray (16/17/18/19) and Two Notch (25/26/27/28) are deliberate**. Verified live as master: Wesmark reads 8/9/10/11 at 1,250/1,500/2,000/2,500 with the $500 increment on the 11 row only; **all 34 stores with tire ladders now anchor the overage on their top row**; Wesmark's kicker is 50/$500 and 100/$1,500; `bonus_plans` has #3308 as D; five flags answered, four open (credit_kicker_at_gold, duplicate_handouts, untracked_inputs, penalty_never_applied). Dry-run 12/12.

55. `pgw_kicker_at_gold_52.sql` — **the credit-app kicker is paid at Gold, confirmed.** *(Migration APPLIED and verified live 2026-09-17.)* The Millwood workbook zeroes the kicker whenever projected GP clears Gold, so the best months lose it; migration 26 called that a defect, paid the kicker at Gold and flagged the call. The user confirmed it, so **nothing recalculates** — one flag row and the standing-decisions comment in `bonusMath.js`. **Six flags answered.** The three still open are `duplicate_handouts`, `untracked_inputs` and `penalty_never_applied` — plus `wesmark_bronze_85`, which is **scoped to Wesmark's location and therefore invisible to any account that cannot see #3938** (it never appeared in the regional session's flag list; that is the whole 9-vs-10 count difference between the live database and the dry-run snapshot). It reads: Wesmark's Bronze is 85% of GP budget where every other store uses 80% — verified live, its 2026 targets are 95/90/85% of budget every month. Migration 28 preserved it as the handout reads and noted that Wesmark's sheet is also the only one built from a budget rather than last year's actuals.

56. `pgw_wesmark_bronze_confirmed_53.sql` — **Wesmark's Bronze at 85% is correct: the store was ACQUIRED EARLIER IN 2026.** *(Migration APPLIED and verified live 2026-09-17.)* One fact from the user closes migration 28's "worth a look at the sheet as a whole": **#3938 Midas Sumter is newly acquired**, so its plan was built differently on purpose. Bronze stays at 85% of budget where every other store uses 80% (Gold 95 / Silver 90 match everyone), and the plan is built from a **GP budget rather than last year's actuals** because the company has no 2025 trading history of its own for the store. **The tire overage line (migration 51) was the ONLY real error on that sheet.** Verified live: seven flags answered, three open, and all twelve 2026 months still read 95/90/85% of budget. **Left as it was, and worth a decision later:** `prior_year_actuals` holds twelve 2025 rows for this store whose gross profit is exactly `0.39245 x sales` every month — a flat margin applied to sales, so modelled rather than measured, loaded by BDC decision. That means Wesmark's vs-2025 report columns compare against a modelled baseline while every other store's compare against real trading. The acquisition explains why; it does not make the comparison a measurement.

57. `pgw_duplicate_handouts_confirmed_54.sql` — **Decker and Pleasantburg are Model B, confirmed.** *(Migration APPLIED and verified live 2026-09-17.)* Each store has two handout sheets, one in Model A format and one in Model B; migration 26 seeded both as B (which is what makes the plan counts work) and left an INFO note. The user confirmed the Model A sheets are stale. Both were already B, so nothing recalculates. **Eight of the ten bonus flags are now answered** (2026-09-17, migrations 50 and 52-54). **The two that remain are one problem in two rows, and neither can be closed by a decision:** `untracked_inputs` — five-star review counts and phone conversion feed real money and nothing records them — and `penalty_never_applied` — the $75-per-app-below-35 credit-app penalty is computed and displayed but never deducted, because Model A's waiver depends on the phone conversion nobody tracks. The second cannot be settled until the first is built.

58. `pgw_company_directory_55.sql` — **Company Directory: every signed-in user reads every store and every active contact; only admin/master write.** *(Migration APPLIED and verified live 2026-09-18 as master and as `teststore`.)* A deliberate exception to `can_access_location()` read scoping, built so it cannot become a side door. **Stores** are read only through `directory_stores()` (SECURITY DEFINER, a fixed 17-column whitelist — number, name, brand, shop number, region/district ids+names, address fields, main phone, hours, hours note — excluding `is_sandbox`); the `locations` RLS is untouched, so a store user still sees one row there. **People** are `directory_contacts` (own display name + title; `employee_id` is admin convenience only and never selected by the app) with `directory_contact_coverage` (store / district / region / company, referencing the existing `locations`/`districts`/`regions` — no parallel list; a check constraint allows exactly the one reference its `scope_type` needs, and a unique index `nulls not distinct` stops duplicate company rows). RLS: contacts readable when `active` or by admin/master; coverage readable by all; insert/update `current_user_role() in ('admin','master')` (the `employee_pay_rates` check); **no DELETE policy, and DELETE/TRUNCATE revoked** so a delete raises 42501 instead of matching zero rows. **Admins edit store fields through `directory_update_store()`, not an UPDATE policy on `locations`** — RLS is row-level, and an admin policy would also let admins rewrite `store_number`, `horizon_shop_number` and `is_sandbox`. Contacts save through `directory_save_contact()`, SECURITY INVOKER so RLS stays the boundary; its coverage argument is the complete desired set, and a row taken off the form is set `active = false`, never deleted (re-adding reactivates the same row). Deactivating a contact leaves its coverage untouched. `locations` gained `address_line1/2`, `city`, `state`, `postal_code`, `main_phone`, `hours` (jsonb, all seven keys `mon`..`sun`, each null = closed or `{open, close}` HH:MM with open < close — checked by `directory_hours_valid()`; whole column null = not entered) and `hours_note`; the structured address was **backfilled by parsing the legacy one-line `address`** (all 38 parsed) and `main_phone` from the legacy `phone` (only Oviedo and Semoran had one). The legacy columns stay, commented as superseded. Coverage on the sandbox is refused by trigger (22023). **38 stores, not the brief's 36** — Oviedo and Semoran arrived in 38/38a. There is still no divested flag, so `directory_stores()` is one more site that must learn it when one exists. Dry-run in PGlite **67/67 with RLS genuinely exercised** (`set role authenticated` per actor — see verification notes). Live: master and store both get 38 stores and 17 columns, no Value Service; as `teststore`, inserts on either table → 42501 RLS, updates on either table and on its OWN location's directory columns → 0 rows (re-read unchanged), deletes → 42501, all three RPCs → 42501 "Only an admin can edit the directory", a deactivated contact is invisible while its coverage row remains.

59. `pgw_employee_profile_pay_history_56.sql` — **Employee profile + effective-dated pay rates.** *(Migration APPLIED and verified live 2026-09-18 as master and as `teststore`.)* **Fixes a real defect: `employee_pay_rates` held ONE undated row per person, and every payroll reader joined it with no date, so a raise re-priced every week ever worked** — the grid, `payroll_pct_summary`, `flat_flags_for_week`, `payroll_to_sales_wtd` and `payroll_to_sales_range`. New `employee_pay_rate_history` (employee, `rate_type` hourly | flat | salary, `effective_date`, `amount`, `source` manual | tech_tracker | legacy; admin/master-only RLS, truncate revoked). **The rule: a pay week uses, per type, the latest effective_date ON OR BEFORE THE WEEK START**, so a change dated mid-week first applies to the next pay week (overtime is whole-week and cannot be priced at two rates). `_pay_rate_at(employee, date)` is the one SQL implementation (revoked from public/anon/authenticated; every caller is SECURITY DEFINER); `lib/payRates.js` mirrors it. **Nothing changed at migration time**: each non-zero old value was copied as a `legacy` row dated 2000-01-01 (live: 14 values → 14 rows; today's rate matched the old table for every employee), and the four readers were regenerated by splicing their SHIPPED bodies with asserted one-for-one replacements (dry run: every reader byte-identical before/after across 3 stores × 5 weeks on a synthetic fixture). **Technician flat rates are still authored only in the Tech Tracker**: `sync_tech_flat_rate()` used to overwrite the undated row with "the rate in force today" (and only when a tech rate was written, so a future-dated raise never took effect on its own); it now mirrors each `tech_pay_rates` row into the history AT ITS DATE (live: 10/10 mirrored). A guard trigger — SECURITY INVOKER on purpose, because inside a definer `current_user` is always the owner and could not tell a person from the sync — refuses manual flat rates for technicians, any hand edit/delete of a mirrored row, and any non-manual source (all verified live, 42501). `employees` gained `hire_date`, `termination_date` (check: not before hire) and `employee_number` (the Employee / ADP ID; not blank, deliberately NOT unique). **Second defect fixed: `payroll_pct_summary` and `flat_flags_for_week` filtered `e.active`, so "Remove" erased a person from PAST weeks too.** A person now counts in a week if they have hours/a timesheet row in it OR `_employed_during()` it (hire on/before the week end; termination on/after its start, or with no termination date, active). Live, this moves no number: the only inactive people with hours are two July technicians, and pre-cutover weeks read no tech_daily. `employee_pay_rates` is kept, frozen, and commented as superseded. As `teststore`: every rate table reads 0 rows, inserts 42501, updates/deletes 0 rows, `_pay_rate_at` 42501, and all four payroll RPCs still work for its own store. **Deploy order: the migration must run before the frontend merges** (the frontend reads the new table).

60. `pgw_directory_v11_57.sql` + `pgw_directory_seed_phones_57a.sql` — **Directory v1.1: Marchex numbers and service types.** *(Migration and seed APPLIED and verified live 2026-09-20 as master and as `teststore`.)* BDC supplied Store_Phone_Numbers_Main_and_Marchex.xlsx with a draft seed that assumed a v1.1 which did not exist: `marchex_phone`, `service_types`, `locations.updated_at` and `locations.active` were all absent (there is still no `active`/divested flag — divestiture remains handled by omission). 57 adds `locations.marchex_phone` and `locations.updated_at` (trigger-maintained), `service_types` (admins extend it at runtime; `code` is IMMUTABLE by trigger and must be lower_snake_case, `label` is free; DELETE revoked — deactivate instead, because stores point at it) and `location_service_types`. **That link table DOES allow delete, unlike every other directory table**: a row means "offers this today", carries no history, and nothing references it. `directory_stores()` was regenerated with two more whitelisted outputs — `marchex_phone` and a `services` jsonb array of {code,label}, aggregated in SQL with inactive types filtered out, so a card needs no second lookup. `directory_update_store()` was **DROPPED and recreated with `p_marchex_phone` (10 args)** rather than widened, because a defaulted argument makes an overload PostgREST picks from at random (migration 36's lesson) — so the old frontend cannot edit a store between the migration and the deploy. New `directory_set_store_services(location, uuid[])` replaces a store's whole list in one definer call, with the same admin check and the sandbox refused (P0002 / 22023). **The seed corrects three things in BDC's draft, all documented in its header:** `3309` → **`3009`** (the roster's SpeeDee Summerville; as drafted that row matched nothing and #3009 would have been the only store with no phone); **#3287 Mt Pleasant and #5253 North Main are given their Marchex numbers but NO main number**, because the source gave both the same main line (843-881-6250) and they are in different towns; and `updated_at` is left to the trigger. **`pgw_directory_seed_phones_57b.sql` closes the held-back pair** *(applied 2026-09-20)*: the user gave North Main's own number, so #5253 takes 843-900-0727 and #3287 Mt Pleasant takes the 843-881-6250 the sheet had repeated onto it — all 38 stores now hold both numbers and none is shared. (The number was first given as 803-900-0727; 803 is Columbia and #5253 is in Summerville, on 843 like its own Marchex line, and the user confirmed 843.) Loaded as supplied and still open for BDC: five stores whose Marchex equals their main number (3303, 3935, 3938, 3211, 3308). Numbers are stored as digits only and formatted for display by `lib/directory.js` formatPhone(). Live: 38 stores / 38 Marchex / 36 main, no punctuation stored, no main number shared, the two pre-existing phones (Oviedo, Semoran) agree with the seed to the digit. Dry run 41/41 with RLS exercised as `authenticated`.

61. `pgw_directory_store_email_58.sql` — **a store email slot on the directory.** *(Migration APPLIED and verified live 2026-09-20 as master.)* Asked for by the user: the store's OWN mailbox, one per store, beside its phone numbers. A person's work email stays on `directory_contacts` — this is the shop, not a person. `locations.store_email` with a loose format check (`something@something.something`, no spaces), stored lowercased and trimmed by the update function, blank clearing it. `directory_stores()` and `directory_update_store()` were regenerated once more, the update function DROPPED and recreated at 11 arguments rather than widened (same reason as 57: a defaulted argument leaves an overload PostgREST chooses from at random), so again the deployed frontend could not save a store edit between the migration and the deploy. Live: the address saves, trims and lowercases; a nonsense address is refused at the database (23514); a store user reads every store's email and is refused every write. Dry run 10/10.

62. `pgw_directory_drop_hours_59.sql` — **store hours come off the directory.** *(Migration APPLIED and verified live 2026-09-20 as master.)* The user: "get rid of the Hours option, I don't want that listed." Weekly hours and the hours note are gone from the card, from the admin form, from `directory_stores()` and from `directory_update_store()`, which is dropped and recreated at **9 arguments** (same overload reasoning as 57 and 58, and the same short window where the deployed frontend cannot save a store edit). **The `hours` / `hours_note` columns and `directory_hours_valid()` STAY, commented as unused**: no store had hours, so nothing is lost either way, and dropping columns is the one step that cannot be undone — if hours are wanted back, the shape and its check constraint are still there and only the two functions and the frontend need to return. Verified in the dry run that hours already stored survive the migration untouched, and live that a store edit still saves with the retired columns undisturbed.

63. `pgw_directory_contact_photos_60.sql` — **photos on the people side of the directory.** *(Migration APPLIED and verified live 2026-09-20 as master, including a real upload.)* Asked for by the user for office staff; they chose to allow a photo for ANY directory contact. `directory_contacts.photo_path` holds an OBJECT PATH, not a URL (a URL would be stale the moment it expired), shaped `<contact id>/<file>` by check constraint. The file lives in a new **private** `directory-photos` bucket following the `training` bucket (migration 9): select for every authenticated user, write for admin/master. **Private on purpose** — a public bucket hands anyone with the link a named employee's photograph, with no login, indefinitely; the app mints short-lived signed URLs instead, as the document library already does. The bucket carries a 5 MB limit and image/jpeg|png|webp, so an upload that dodges the form is refused by storage itself (verified live: a text/plain upload came back 415). Nothing else changed — no function signature moved, so unlike 57-59 there was no window where the deployed site could not save. Dry run 15/15; live: upload, signed URL fetch, type refusal, removal leaving the URL 404.

64. `pgw_adjustments_reason_62.sql` — **every non-zero Adjustment needs a written reason.** *(Migration APPLIED and verified live 2026-09-21 as master, on the Value Service sandbox.)* Asked for by the user: a comment on the Adjustments box of the tic sheet's Sales breakdown, **required with any amount**. `daily_kpi.adjustments_note` plus its own `adjustments_note_updated_by/_at`. The rule lives in `daily_kpi_adjustments_guard()` (regenerated from 48's body): same editors as the amount (district manager or above); a signed-in user cannot save a non-zero amount with a blank reason, nor blank the reason while an amount stands (23514 "Add a reason for the adjustment."); blanks are trimmed to null. **Checked only when the amount or the reason CHANGES**, so the days that already carry an amount and no reason (Millwood July, the Florence import, the sandbox copy) stay editable for every other line. **No signed-in user (SQL Editor, imports) is exempt**, exactly as 48's role check is. **A reason-only edit does not move `adjustments_updated_at`**, so it never flags a Horizon re-send — the numbers did not change; an amount edit still flags exactly as in 48. The panel mirrors the rule before sending (red "Add a reason for the adjustment." under the box); store users see the reason read-only, or "No reason recorded" on an older adjusted day. **Deploy order: run the migration before the frontend merges** — the tic sheet selects the new column. Dry run 19/19 with RLS exercised; live: untouched save of an older adjusted day passes, an amount change with no reason is stopped in the panel and nothing is written, a reason-only save stamps the reason and leaves the amount stamp and re-send status alone, and a direct API write with no reason is refused 23514. **Left behind by verification:** the sandbox's 2026-07-01 row carries the reason "Verification test (sandbox) - reason only".

**Data load, 2026-09-21 — Midas Florence (#3377) tic sheets January–August 2026** (not a migration; generated from `Master Tic Sheet 2026\Charleston\Florence\*.xlsm`, file kept outside the repo because it is real sales). 206 business days of `daily_kpi` + service units, 653 `tech_daily` rows over nine techs, pay rates (Thomas McElwain 34/17 → 38/20 from May, Grayson Smith 34/17 → 38/20 from August) and Keith Walters' −58.40 other pay in the week of 2025-12-28. Every month matched the workbooks' own week and month totals to the cent before load and again live. Chris Grey, Keith Walters, Malcolm Pollard and Jon Lockie were loaded as former employees ending on their last tic-sheet day; Garrett Hyman, Richard Doyle, Grayson Smith, TJ Howell and Thomas McElwain are active because nobody could say who still works there. **Open:** TJ Howell's flat rate is 0 on the sheet; April 9 carries a −6,159.43 adjustment; Estimates (109–188 a month) have no portal field and were not loaded.

66. `pgw_matt_reports_config_61.sql` + the **Daily Reports** tab — **Matt's reports, Phase 1 (Daily Scorecard, Market Summary, Tire Contest).** *(Migration APPLIED and verified live 2026-09-21 as master, after 62 and 63 — numbered 61 because it was written first; it only depends on 60.)* Config is data: `markets` (Matt's six groups — NOT the districts: SpeeDee Lexington is its own market; Florida holds 8 stores including #2320 Semoran and #2322 Oviedo, by the user's call), `store_report_profile` (market, report row order, Matt's display name, name-fill override), `store_report_config` effective by month with carry-forward (`store_report_config_for`), `report_settings` (tier %, colour bands, tire payouts), admin-only `market_bonus_brackets`, `prior_year_actuals.tires` + a September 2025 seed, the 2025 holidays, and `open_days_between` / `report_default_date`. Days open, GP and sales budgets are REUSED from `bonus_monthly_targets`; every daily number comes from `report_build()`, so there is one definition of sales and GP. **Sales on the scorecard is `gross_sales` (includes Adjustments), matching the "Gross Sales" line of the stores' Summary tab** — e.g. North Main's Aug MTD $178,010 = sales $177,260.61 + adjustments $749.40. Frontend: `lib/scorecard.js` (numbers), `lib/scorecardRules.js` (ONE rules engine driving both the screen colours and LIVE Excel conditional formatting that compares each cell to its own row's goal cell, so editing a goal in the export recolours it), `lib/scorecardWorkbook.js` (Current Month tab in Matt's columns), `hooks/useScorecard.js`, `components/reports/ScorecardView.jsx`; `ReportsView` now tabs **Daily Reports | Report Builder** and Task 10's preset chips are gone (superseded). Tier tokens `--tier-gold/silver/bronze-*` in `index.css`. A store that has not entered its tic sheet shows "·", never 0, and is never coloured. `scorecard.test.mjs` 88/88, including an Excel-formula evaluator checked against the screen colours; parity vs Matt's workbook 889 match / 46 expected / 0 unexplained (harness kept outside the repo — real sales). Live (2026-08-29): all five VERIFY checks pass (markets 8/6/5/10/1/8, report order 1–38, 36 September configs, Sep 2025 = 25 open days, "yesterday" of Mon 9/21 = Sat 9/19); "9 of 38 stores entered" = exactly the imported Charleston stores; cars and sales match the workbooks; the export builds "Current Month 2026-08-29.xlsx" with 15 live colour-rule blocks.

65. `pgw_speedee_extra_categories_63.sql` — **four more SpeeDee tic-sheet categories, all portal-only.** *(Migration APPLIED and verified live 2026-09-21 as master.)* The SpeeDee Master Tic Sheets count services the portal's SpeeDee list lacked; the user chose to keep them. The existing Midas-only `local_su_15k_critical_sys` is switched on for SpeeDee (display_order 145), and three new SpeeDee-only categories are added: `local_su_ac_odor` "AC Odor Service" (15), `local_su_misc_inspection` "Misc Inspection" (195), `local_su_spark_plugs` "Spark Plugs" (235) — slotted into the gaps, nothing renumbered. **All four keys start `local_`, and the Horizon payload sends `kpi_su_*` only (`supabase/functions/horizon-upload/payload.ts`), so nothing sent to Horizon changes.** Live: SpeeDee list 35, Midas still 30; Summerville's March tic sheet shows the four columns with the workbook's totals (Misc Inspection 19, Spark Plugs 4). Dry run with the SpeeDee imports all passing, including the imports refusing to run before 63.

67. `pgw_tech_slots_20_64.sql` + Tech Tracker picker — **up to 20 tech slots per store (was 9).** *(Migration APPLIED and verified live 2026-09-21 as master.)* The Columbia tic-sheet imports needed it: Two Notch ran ~15 techs at once in January 2026, Lake Murray 11, Decker and Gervais ~10. The migration drops whatever check constraint on `tech_slots` mentions `slot_index` (migration 24 left it unnamed) and adds `tech_slots_slot_index_check` = 1..20, matching Horizon’s 20 slots. Nothing else keys on `slot_index`: migration 29’s RPCs, `_tech_days`, payroll and the Horizon upload all join slots by id (the upload never reads `slot_index`). `TechTrackerView.jsx` shows 9 slot buttons and grows to one past the highest slot in use (max 20); the picker already wraps. Unchanged: a NEW slot 9 is still created as Manager/SA. Live: Two Notch shows 20 named slots; Decker 20, Lake Murray 16, Gervais 13.
68. `pgw_bonus_inputs_dm_edit_65.sql` + Bonus Tracker — **Google reviews and phone conversion are a district-manager edit.** *(Migration APPLIED and verified live 2026-09-24 as dm@.)* Asked for by the user. Replaces migration 27's split: the review count was any store user's, phone conversion admin-only; both are now **district / regional / admin / master**, scoped by `can_access_location()` (a DM edits only their district). Store users keep read access and lose write access to `bonus_monthly_inputs` entirely (insert/update policies now require the DM tier). The column guard keeps only the **Midas referral GP credit** admin-only. Existing store-entered review counts are untouched. UI: store users see "View monthly inputs", read-only fields ("Set by your district manager.") and no Save. **Latent bug from 27 fixed in `useBonusTracker.js`:** the upsert sent every column, and on the insert half the guard resets `referral_gp_credit` to 0, so a non-admin saving a Model D store with an admin-set credit hit "Only an admin can set the referral GP credit"; non-admins now omit that column. Dry run 13/13 in PGlite with RLS exercised (store refused, DM saves own stores only, DM referral refused/forced to 0, RM saves in region, master moves all three, admin credit survives a DM save). Live as dm@ on Millwood's July row: reviews + phone conversion saved (then restored to blank), referral credit refused 42501; panel shows both fields editable with Save.
69. `pgw_directory_dm_phones_66.sql` + Directory — **District managers and up can edit store phone numbers.** *(Migration APPLIED and verified live 2026-09-24 as dm@.)* Asked for by the user. New definer `directory_update_store_phones(location, main, marchex)`: **district / regional / admin / master**, scoped by `can_access_location()` (a DM edits only their district; the sandbox is unreachable to non-admins). Authorization runs first, so a refused or unknown id gets 42501, never a "no such store". `directory_update_store()` (address, email, both phones, admin-only) is **untouched**, so nothing broke between migration and deploy. UI: DMs/regionals see the pencil only on their own stores, opening a phone-only `StorePhonesModal`; admins keep the full editor. Dry run 14/14 in PGlite with RLS exercised (store refused, DM saves own/refused outside district + sandbox + unknown id, address/email untouched, blank clears, RM saves in region, full admin editor still refused to DM). Live as dm@: exactly 6 pencils (Columbia East), #3009 Charleston refused 42501 and unchanged, full admin editor refused 42501; Millwood main phone set to 8035550199 through the form and restored to 8033861938.
70. `pgw_employee_birthdays_67.sql` + Employee Schedule — **birthdays and work anniversaries on the calendar.** *(Migration APPLIED and verified live 2026-09-24 as master.)* Asked for by the user. `employees.birth_month` / `birth_day` (**month and day only — the birth YEAR is deliberately never stored**; check constraint allows Feb 29, refuses half-set pairs — the explicit NOT NULLs matter because a NULL check result passes) and `rehire_date` (must be ≥ `hire_date`). **Anniversaries count from `rehire_date` when set, else `hire_date`** (user decision); `hire_date` stays the ORIGINAL hire. No new policies — `employees` is already location-scoped. UI: `celebrationsByDate()` in `lib/scheduleMath.js` (tests in `scheduleMath.test.mjs`, 9/9) puts a cake / party-popper line in each day cell and full names in the day modal; active employees only; anniversaries from 1 year; Feb 29 shows on Feb 28 in non-leap years. Employee profile gains Rehire date and Birthday (month + day selects). **Data:** loaded from the ADP Employee Census by `pgw_employee_dates_load_67a.sql` — **kept OUT of the repo** (birthdays keyed by employee id), like the tic-sheet loads. 147 employees matched by name + store (user confirmed six uncertain pairs, e.g. Carter = Cash Cantrell); 27 with rehire dates; Corporate #1515 (25) skipped. **Gaps reported to the user:** 114 census store staff have no portal employee row; 76 active portal employees are not in the census (incl. test rows at #5254); 5 census people are inactive in the portal; Khalil Goode loaded onto his active #3598 row. Live: 147 hire / 147 birthday / 27 rehire, 142 active; Millwood shows Bradley Jones's birthday 9/6 and Cash Cantrell "1y" on 7/21/2026 (hired 7/21/2025); Deandre Palmer (hired 7/2/2026) correctly absent.
71. `pgw_home_office_68.sql` + Payroll / Shell — **the Home Office (#1515), a non-revenue location.** *(Migration APPLIED and verified live 2026-09-25 as master.)* Asked for by the user. **Decisions:** holds corporate staff, runs Payroll + Employee Schedule, can have its own logins (role `store` pointed at it); store number **1515** = ADP's Corporate #1515; **no district**, so only admin/master (and its own logins) can see it; **not in the Directory**; a new **`office` position valid only there** (and the only one allowed there); its payroll is **excluded from company payroll %**. New `locations.is_home_office` (at most one; can't also be sandbox; `horizon_shop_number` must stay null) — deliberately NOT `is_sandbox`, which hides a location from its own users. `brand` stays `midas`: the Home Office runs the Midas payroll engine with the tech/sales columns hidden, so no third brand value. `provision_horizon_slots` skips it (0 slots). `is_store_manager` may now be set on `office` rows (any number — the one-per-location index is narrowed to managers), labelled "Salaried"; pay math unchanged. **Rollups:** `and not l.is_home_office` spliced into the shipped bodies of `_tech_pay_range`, `_report_tech_daily`, `_report_grain`, `_report_store_scalars`, `report_build` (2 sites), `dashboard_range_metrics`, `payroll_to_sales_range` (6) and `directory_stores` (generated by a build script with asserted match counts). Single-store readers untouched, so its own Payroll page works. **Frontend:** `lib/homeOffice.js` (+ test, 31/31) — Dashboard / Cash Drawer / Tic Sheet / Tech Tracker / Bonus are hidden while it's selected and fall back to Payroll; `revenueStores()` keeps it out of the Reports store picker, the closeout range export and the dashboard scope; auto-select skips it. **Deploy order: 68 before the frontend** (`AuthProvider` selects `is_home_office`). PGlite 37/37 incl. a control (flag off → payroll_to_sales_range counts 39 stores). Live: one row (1515, midas, no district, no shop), 0 Horizon slots, directory 38 without it, payroll_to_sales_range and report_build count 38 stores without it, trimmed menu + office-only grid on screen, `tech` at the Home Office and `office` at #3303 both refused (no rows written). **Open:** the 25 Corporate #1515 people in the ADP census are not loaded yet.
72. `pgw_shift_call_out_69.sql` + Employee Schedule — **"Call Out" shift type.** *(Migration APPLIED and verified live 2026-09-25 as master.)* Asked for by the user. One more row in the company-wide `shift_types` catalog (migration 30): **Call Out / CO**, `counts_toward_hours = false` (user decision — the person did not work it; the shift keeps its times, shows "not counted in hours", and drops out of the weekly total), `is_copyable = false` (month duplication treats it as time off), sort 45 right after Sick. **Its own colour** (user decision): new token `shift-lime` `#9BE06B` in `index.css` / `tailwind.config.js` / `lib/shiftTypes.js` allowlist — hue 95, 53 degrees clear of the warning yellow and 74 of the accent per migration 30's rule, far lighter than HOL green, 10.73:1 on the weakest surface; Excel fill `FFE8F6D8`. Legend, shift dropdown, weekly total and Excel export all read the catalog — no screen code changed. Either deploy order is safe (unknown token falls back to neutral). PGlite 5/5. Live: row present; legend swatch renders rgb(155,224,107); the add-shift dropdown lists "CO · Call Out — not counted in hours" and the form reads "8 scheduled hours · not added to the weekly total" (form closed without saving — no schedule rows written).
73. `pgw_who_sold_what_70.sql` + Reports — **Who Sold What** (Matt's service penetration report, from his `Who Sold What Template.xlsx`). *(Migration APPLIED and verified live 2026-09-25 as master.)* New **Reports → Who Sold What** tab: per store, each service's units ÷ cars (report_build `cat_units_*` / `ro_count` — identical to the existing `cat_pct_*` measure), against a goal, in three sections (Low Hanging Fruit / Flushes / Tires & Repairs), each with the template's average of 5 (section 1 excludes Prem Oil, as his R4 does), a rank across every store shown (ties share), and the change vs the previous month's average. Market and PGW rows POOL units ÷ cars. **User decisions:** all 38 stores by Matt's markets + market filter; a month vs the month before; untracked services (Starting & Charging, TPMS, Drivetrain, Electrical, Emissions) left out; goals saved + admin/master-editable. `service_penetration_goals` (25 rows: section, order, label, measure pct|per_day|count, goal, in_average) — RLS read-all, update admin/master, **column grant on `goal` only** (layout is migration-owned), no insert/delete from the app, re-running keeps edited goals. Name map: Diff = Gear Box Flush, Fuel = Fuel Injection Flush, Suspension = Steering & Suspension. Colours (my choice, modelled on his Aug 23 tab's live rules): green ≥ goal, amber ≥ 75% of goal (`CLOSE`), red below; blanks never coloured. Excel export in his side-by-side layout with LIVE formulas off a Counts tab (%, average, RANK, change) and CF bands against the goal row. `lib/whoSoldWhat.test.mjs` 29/29 incl. the template's Main St row (R4/AJ4/AY4). PGlite 12/12. Live: August 38/38 stores, Millwood screen = `cat_pct_*` to the percent (381 cars; Prem 34%, Air 6%, Tires 36%); goal edit saved + heading updated, then restored (Timing Belt 8% → 9% → 8%). **LOF fix — `pgw_who_sold_what_lof_71.sql`:** verified live, LOF / day read ~0–1/day everywhere because stores log most oil changes as LOF Premium; **user decided 2026-09-25 the column counts LOF + LOF Premium** ("LOF + Prem / day", goal 7). Kept as data: `service_penetration_goals.also_counts text[]` (trigger-checked against `service_categories`, cannot name itself; not app-editable). `lib/whoSoldWhat.js` sums `serviceKeysOf()`; the export's Counts tab has one column per SERVICE and the LOF cell is `(LOF+LOF Premium)/days`. Prem Oil's own column is unchanged. PGlite 5/5; test 30/30. **Deploy order: 71 before this frontend** (the hook selects `also_counts`). *(71 APPLIED and verified live 2026-09-25 as master: Millwood August 6.1/day = (27 LOF + 131 Premium) / 26 days, screen = database; stores now span ~3–8/day with a mix of green / amber / red.)*

**Data load, 2026-09-21 — the rest of Charleston district, January–August 2026** (not migrations; same approach as Florence; generator, parsed data and SQL kept in OneDrive `Tic Sheet Import (Claude)\` with a `STATUS.md` of every decision — real sales, outside the repo). Column mapping comes from each workbook's own **Excel Uploader** tab (the Horizon mapping), tech tabs from each day's own Summary labor formula. **Midas #3182 Trolley, #3287 Mt Pleasant, #3302 Sam Ritt, #3385 Rivers, #3938 Sumter (Wesmark, from Feb 25), #5253 North Main** — every store-month matched live (47). Tech slots are capped at 9: anyone with August hours keeps a named slot, leavers get their own while there is room, the rest share **"Former techs"** placeholder slots with `employee_id` set on each row (payroll and reports stay per-person; only the Tech Tracker screen groups them). Workbook tabs with hours but no name load onto **"Unnamed tech"** slots (labor counts toward sales; no pay). Not loaded: North Main's March "day 33" and July "day 32" overflow rows, Rivers' Jahiem Venning Jul 11 (duplicate of Adley Piccolo's day), Engine Oil Cleaner. **SpeeDee #3025 James Island and #3009 Summerville** — every store-month matched live (16). By the user's decision SpeeDee loads **store labor only**, on one **"SpeeDee store labor"** placeholder slot (daily Labor Sales, all tech + GS hours, flag hours = labor ÷ 150 as the workbook computes it) — no SpeeDee employees or pay, because the Tech Tracker cannot price SpeeDee commission/GS pay. Both brake-pad tiers are Brakes; Parts Cost is the workbook's parts-only column. **Open: #3029 North Charleston SpeeDee is held** — on 51 tire days its parts-only cost column was typed over with the combined parts+tire figure ($12,034.34 over Jan–Aug); the user is asking their spreadsheet specialist before choosing.

**Data load, 2026-09-21 — Columbia district (12 stores), January–August 2026** (not migrations; Charleston generator adapted, parsed data, SQL and `STATUS.md` in OneDrive `Tic Sheet Import (Claude)columbia`). #3936, #3305, #3229, #3937, #3278, #3276, #3303 Millwood (its July 2026 test-seed slots cleared first, guarded), #3308 SpeeDee Lexington (store labor only), and — after 64 — #3935, #3984, #3979, #5254. Every store-month verified live against the workbooks (days, ROs, parts, tires, labor, units). Hardscrabble’s tic sheet sits one column left; its first load missed the tic rows and was regenerated.

**Data load, 2026-09-22 — Florida district (8 stores)** (not migrations; same generator, work folder OneDrive `Tic Sheet Import (Claude)lorida` with `STATUS.md`; the source folder is spelled `Master Tic Sheet 2026Flordia`). January–August for #3548 Atlantic Blvd, #2321 Beach Blvd, #3211 Gainesville, #3136 Lem Turner, #3292 Orange Park, #3111 Sunbeam Rd; **August 17–31 only** for the newly acquired #2322 Oviedo and #2320 Semoran (their workbooks start there). All within 9 tech slots. Every store-month verified live against the workbooks (days, ROs, parts, tires, labor, units). Typo readings and name merges are listed in `STATUS.md`.

**Data load, 2026-09-22 — North district (8 stores), January–August 2026** (work folder `Tic Sheet Import (Claude)
orth`, `STATUS.md`). #3485 Capitol Heights (16 tech slots), #3923 Clinton, #3473 Duke St, #3726 Fairfax, #3593 Forestville (11), #3831 Manassas (17), #3598 Rhode Island Ave, #3296 Temple Hills — the three larger stores rely on migration 64. Every store-month verified live. **Manassas workbooks have a Summary labor formula slip** (May 30, Aug 29 and the July overflow row point one row up on one tech tab, double-counting that tech’s previous day): the portal carries the tech tabs as entered, so it is lower than the Summary on those days (May 30 by $230.27, Aug 29 by $822.08) — Horizon likely received the inflated figures. Manassas’s August uploader lost its Declined Sales formula; declined is read from the tic sheet column.

**Data load, 2026-09-24 — #3029 North Charleston SpeeDee, January–August 2026: the last store, and a corrected parts-cost reading.** Its Summary has two parts-cost columns side by side. The first (J) carries the template formula `=K-M` (second column minus tire cost); the second (K) is what the store types. On 51 tire-selling days J had been typed over with K, which looked like the error — **it was the opposite.** The user confirmed 2026-09-24 that the formula days are the wrong ones: **K is parts-only and never included tire cost, so `K-M` subtracts it twice.** The store's own numbers agree — on its 144 days with no tire sales J and K are identical at **61.1% parts GP**, and on tire days K holds 61–64% while `K-M` would imply 68–73%. So `gen3.mjs` reads the SECOND parts-cost column for 3029 (`SECOND_PARTS_COST_STORES`); only the 12 formula-intact days changed, **+$2,013.69** of parts cost, and the 51 typed-over days were right all along. Live: 8 months match, all 12 days correct, year parts GP **61.9%**. The same template formula sits in the other SpeeDee workbooks and only distorts days with tire sales — James Island and Summerville have none of the typed-over pattern and Lexington records no tire sales at all, but their parts cost would be understated the same way if tire sales appear.

**Tic-sheet history status: all 38 stores loaded and verified live for January–August 2026** (Charleston 10, Columbia 12, Florida 8, North 8).

## Frontend build order

1. **App shell** — Vite/React/Tailwind scaffold, real Supabase email/password auth (`AuthProvider.jsx`), role-scoped store picker, sidebar/header/breadcrumb (`Shell.jsx`). "Preview as" role switcher removed — role comes from the logged-in profile.
2. **Payroll** (`HoursView.jsx` / `SpeedeeHoursView.jsx`, `usePayroll.js`, `lib/payrollMath.js`) — brand-aware weekly payroll (nav tab renamed "Employee Hours" → "Payroll"). Follows `store.brand`; pay rates/dollars never reach a store user (enforced by RLS, not the UI), stores see percentages only. CSV/print/xlsx export.
3. **Cash Drawer** (`components/drawer/*`) — full deposit-sheet form (denomination counts, daily sales summary, over/short, 4 line-item tables), saved-closeouts list, read-only detail modal, CSV/summary-CSV/print export. All math in `lib/drawerMath.js`, matches the original Excel formulas exactly (including the two "known quirks" — `poa_checks_total` feeds nothing, `sales_tax` doesn't flow into `total_sales`).
4. **Documents** (`DocumentsView.jsx`) — per-store folder tree, upload/open/delete, built on a reusable `FileBrowser.jsx` + `useFileLibrary.js` hook.
5. **Training** (`TrainingView.jsx`) — same `FileBrowser`/`useFileLibrary` reused with no `locationId` (shared across all stores), write access restricted to admin/master.
6. **Dashboard** (`DashboardView.jsx`) — latest cash drawer closeout, this week's hours, document count, recent-activity feed.
7. **User Management** (`components/users/UsersView.jsx`, master-only) — list all users, create logins, assign role + store/district/region, revoke access. Notable design choice: **no `service_role` key anywhere in this app.** Creating a user goes through a throwaway Supabase client (`persistSession: false`) so it never disturbs the master's own session; a random temp password is shown once. Revoking access just clears role/scope (RLS then denies everywhere) rather than deleting the Supabase Auth login.
8. **Employee Schedule** (`components/schedule/*`, `useSchedule.js`) — per-store monthly shift calendar, add/edit/delete shifts against the `employees` roster. No pay data.
9. **Daily Tic Sheet** (`TicSheetView.jsx`, `useMonthlyTicSheet.js`, `useMonthlyGoals.js`, `lib/workdays.js`) — Midas store manager's monthly entry, laid out as the **month grid** the managers already use (reworked from the original one-day form; the old `useDailyKpi.js` was removed). One row per day of the selected month (Sundays, holidays, and future days are visible but muted / not editable), one column per service category in `display_order` (30 for Midas, vertical headers so they fit), then the day-summary columns. Frozen day column + header band (the **Actual % of Cars** band = MTD units ÷ MTD ROs; per-category **Goal %/Goal Units** rows are hidden until penetration goals exist); horizontal scroll for 30+ columns; **Totals** row (month-to-date sums) at the bottom. Cells edit in place with **per-cell autosave** (Tab across a day, Enter down a column) — no Save/Submit button (`submitted_at` stays in the DB, unused by the grid). Day-summary columns: Repair Orders, First Time Customers, **Sales** (computed = labor+parts+tires+supplies+groupon+discounts, where `sales_discounts` is stored **negative** so it reduces Sales; opens a per-day breakdown panel with those 6 revenue lines + Parts Cost/Tire Cost, showing Sales total and Gross profit), Declined Sales, CC Apps, Credit $, and three read-only computed columns — Total Potential (Sales+Declined), Ave Estimate/Car (Total Potential÷ROs, blank when ROs=0), Sales Capture Rate (Sales÷Total Potential, blank when 0). The **goals summary strip** still sits above the grid; its "GP before labor" = Sales − Parts Cost − Tire Cost. Scope is data entry only: no bonuses, technician entry, pay math, or uploads. Verified live: 30 columns correctly named/ordered (15K Critical Sys between F/I Flush and Gear Box Flush), 31 rows with muted Sundays/July 4, autosave round-trip, totals, Actual %, the computed columns, zero-RO blanks, and the 8-field breakdown panel (Sales/GP math). Cross-store isolation rides on the unchanged `daily_kpi` RLS proven earlier (403 `42501`).
10. **Deployment** — GitHub repo created, pushed, Vercel project imported with Root Directory set to `pgw-portal`, env vars configured, auto-deploys on push to `main`.
11. **Tech Tracker** (`TechTrackerView.jsx`, `useTechTracker.js`, `lib/techPayMath.js`, `lib/grossProfit.js`) — new nav tab. Per-technician monthly view: five Sun–Sat week blocks, columns `Work Day # | Day | Hrs Worked | Flag Hours | Labor Sales | ELR | Guar Pay | Commission | O/T | Total Pay`, weekly + month totals, the "on overtime in N more worked hours" countdown. Managers type only Hrs/Flag/Labor (autosave on blur, Enter moves down); **pay columns render only for master/admin** and are enforced by RLS (store users have no rates, so no endpoint returns pay). A store-level **technician summary** table (Summary!P25:T34 — Proficiency, ELR, and, master-only, Labor GP % + Cost/Hour) and a master **effective-dated rate editor**. `techPayMath.js` mirrors the SQL pay engine exactly (incl. the deliberate daily-allocation defect, commented); `grossProfit.js` completes portal GP. Both have `*.test.mjs` (run with `node`, 35 assertions). **Part 4 wiring:** the tic sheet's **Labor Sales is now read-only** (locked, sourced from `tech_store_daily`) — `useMonthlyTicSheet` exposes `laborSalesByDate` and every sales figure adds it to the remaining `daily_kpi` lines; the goals strip now shows **full gross profit** (labor cost deducted via `computeGrossProfit`), replacing the old "GP before labor" described in feature 9. Payroll's flat-rate field is read-only for techs (mirrors `tech_pay_rates`). Verified live on Millwood #3303 July 2026: Phillip Brayboy wk1 $547.20 / wk2 $906.40 / wk3 $896.08 (alloc $913.15) / month $3,905.20, store labor cost $15,640.19, avg cost/sold-hr $42.23, shop proficiency 49.9%, tic-sheet Labor Sales $3,887.98 (Jul 1) locked. Note: store ELR reads $158.19 until `daily_kpi` groupon is entered (the $156.92 target is groupon-blended, `(labor+0.5·groupon)/flag`).
12. **Tic sheet totals + self-correcting goals** (`lib/ticSheetMath.js` + `ticSheetMath.test.mjs`, rewritten `TicSheetView.jsx`, extended `useMonthlyTicSheet.js`, `moneyCell()` in `lib/format.js`) — the month grid now matches `Maint Tic Sheet` end to end. Day rows are grouped into **Sunday–Saturday week blocks**, each followed by three rows: **Weekly Totals** (column sums; the two ratio columns *recompute* — Ave Estimate/Car = weekly Potential ÷ weekly ROs, Capture Rate = weekly Sales ÷ weekly Potential), **New Goal**, and **Weekly % of Cars** (weekly units ÷ weekly ROs). At the bottom: **Monthly Total** (the sum of the Weekly Totals rows, ratios recomputed) and **PACE** = MTD ÷ (days elapsed ÷ days open), with the source's legend line under the grid. *New Goal* is cumulative remaining units — `monthly_goal = goal % × projected ROs`, then each week subtracts its own total from the previous remainder, so slipping in week 1 raises week 2's target; **negatives display with the minus sign** (Millwood finished July at −43.6 tires / −36.9 LOF Prem) and are never clamped. `projected_repair_orders = MTD ROs ÷ days elapsed × days open`, where *days elapsed* counts **entered** days, not calendar days. The **header band** (frozen with the day column) is five rows: Goals % of Cars and Average Sale (both entered — inline-editable for admin/master, read-only for stores), Actual % of Cars, Actual Sales (units × average sale) and Goal Units. **Zero Dollar Tickets** replaces First Time Customers as a day-summary column with MTD count, % of ROs in the band (flagged red over the store's goal — Millwood 15.9% vs 10%) and a PACE figure. Discounts are entered **signed** (the panel no longer coerces to negative). Money cells render through `moneyCell()`, the JS mirror of `CURRENCY_FMT` (`$#,##0.00;-$#,##0.00;"-"??`), so screen and Excel export agree. **Two source bugs deliberately not replicated:** A/C Refresh / Catalytic Converters / 15K Critical Sys point their goal formula at an empty cell in the spreadsheet and read zero forever — here every category uses the same projected-RO source; and the sheet hardcodes five week blocks, which silently drops the last two days of a 31-day month starting Saturday (August 2026) — `monthWeekBlocks()` emits as many blocks as the month needs. **Sales now excludes Groupon**, matching the source's Sales column (`Summary G+H+J+L+N`, no M) — this changes the formula recorded under migration 22; Groupon is still entered in the breakdown panel and still feeds gross profit, where the source splits it 50/50 across labor and parts. `ticSheetMath.test.mjs` runs the whole Millwood July month offline (`node src/lib/ticSheetMath.test.mjs`, 62 assertions).
13. **Bonus Tracker** (`components/BonusView.jsx`, `hooks/useBonusTracker.js`, `lib/bonusMath.js` + `bonusMath.test.mjs`) — new nav tab, all four models. Every model measures **projected month-end gross profit** — `(MTD GP ÷ days elapsed) × days open`, where *days elapsed* counts entered days — so it answers "what will I earn if the rest of the month looks like this" rather than being a scoreboard; both figures are shown side by side and converge at close. GP is the same `computeGrossProfit()` the tic sheet's goals strip uses, so **technician labor cost is already deducted** — before the tech tracker existed this ran ~20% high, enough to move Millwood two full tiers. `bonusMath.js` contains **no number at all**: rates, splits, thresholds, scales and policy scalars are all arguments loaded from migration 26. **Two standing decisions, both flagged on screen:** (1) penalties are calculated and displayed but **never deducted** — the pool is shown gross with "if it were applied the total would be $X" beside it, because Model A's waiver turns on phone conversion, which nothing tracks; (2) the Millwood workbook zeroes the credit-app kicker whenever projected GP clears Gold, so its best months lose it — that is a defect, and the kicker is paid at Gold here. Unpaid lines say **why**: *locked* (a GP gate is unmet), *not reached* (the measure missed a rung), *not entered* (nobody has typed it) — an un-entered figure is never rendered as $0.00. Every screen carries the estimate disclaimer. Conflict flags are location-scoped (Millwood shows 8, #3308 shows 9 — the extra being its own Model C-vs-D question). Verified live on Millwood July 2026: Bronze, pool $4,579.04 = $3,329.04 base + $1,250 tire, split $2,976.38 / $1,144.76 / $457.90; $2,100 penalty shown not applied; pushing GP past Gold gives $9,050 with tire and credit intact. Item 9 proved end to end by changing one technician day's hours: labor cost −$34.00 → GP +$34.00 → pool +$1.36 → GM +$0.88, then restored exactly. A store user sees one location across every bonus table, gets 403 `42501` inserting another store's rows, and 0 rows changed updating targets or policy. 101 assertions in `bonusMath.test.mjs`.

14. **Tech Tracker slot management** (`SlotManager` in `TechTrackerView.jsx`, `reassignSlot` + `countRowsFrom` in `useTechTracker.js`) — replaces the old assign-only affordance, which handled empty slots only. An admin can now assign an active employee to any of the nine slots, clear a slot back to empty, or give an unstaffed slot a placeholder label. The technician dropdown **disables anyone already holding another slot** and names it (`Alan Barron (slot 4)`); the label field is disabled while an employee is selected so the two can never both be set. An **effective date** (defaults to today) bounds how far back a change reaches, with a live preview of how many entered days would be re-attributed — `2026-07-01 → 24 days`, `2026-07-20 → 10 days`, `2026-08-01 → "No days entered on or after 2026-08-01 — history is untouched either way."` Those counts are the same number the RPC actually moves. **Dropped the `is_manager_or_sa` inference**: the old code set it with `/manager|sa/i.test(label)`, so a placeholder named "Sam" or "Rosa" would have been flagged Manager/SA — slot 9 now keeps the flag from the database and updates pass null so it is never disturbed. Store users see no slot controls at all and keep full hours/flag/labor entry.

    **Unattributed days notice** — hours typed against a slot that had nobody in it carry no pay and no labor cost, which is correct but was invisible. A banner above the slot selector now names them: *"10 days this month have no technician"*, per slot, with the dates (first 8 then `+N more`) and a click-through to that slot. Two shapes count — the slot holds someone **now** but a day predates the assignment (`Slot 1 · now Phillip Brayboy`), or the slot is **entirely empty** and somebody typed into it anyway (`Slot 1 · still empty`). **A placeholder slot is deliberately excluded**: a labelled, unstaffed slot like `MANAGER OR SA` is *meant* to carry no pay, so flagging it would leave every store with a permanent warning it can never clear — Millwood's slot 9 has two July days with real hours and correctly stays silent. Derived from rows the hook already holds, so no extra query; scope is the month on screen. Admins get the fix hint (reassign with an effective date on or before the earliest day listed); store users see the notice without it, since they cannot act on it.

15. **Schedule shift types + month duplication** (`lib/shiftTypes.js`, `lib/scheduleWorkbook.js`, `schedule/DuplicateMonthModal.jsx`, extended `ScheduleView`/`DayDetailModal`/`useSchedule`/`scheduleMath`) — a legend above the grid, a colour bar and abbreviation on every typed shift block, and a type picker in the day modal. **An untyped shift renders with no bar and no abbreviation, byte-identical to before.** `weekSummary` gained a `typesById` argument and skips any type with `counts_toward_hours = false`, so unpaid time off and open/unassigned drop out of both the week total and the per-employee rows. Seven new theme tokens in `index.css` + `tailwind.config.js`, **all verified against WCAG AA rather than eyeballed** — weakest is `shift-slate` at 5.52:1 on `--surface-overlay`. The first pick for Open/Unassigned (`#F85149`) was **rejected at 19° of hue from the accent orange** and replaced with magenta `#F778BA` at 52°; nothing sits within 40° of the accent or the warning yellow at usable saturation. `color_token` names a CSS custom property directly (`shift-blue` → `var(--shift-blue)`), so no Tailwind safelist is needed and no class name is built by concatenation. **New Excel export** (`buildScheduleWorkbook`) — month grid, legend with fills, `[ABBR]` in every typed cell's text, per-type fills from `export_argb`, and an hours column honouring `counts_toward_hours`; a day holding more than one shift takes no fill, since a single fill would misrepresent it and the per-line abbreviation already says what each shift is. After migration 31 an unassigned shift renders as **"Unassigned"** in italic (screen) and in the cell text (export) rather than an em dash, which would read as missing data instead of a deliberate gap; `weekSummary` buckets any unassigned shift under "Unassigned" so a null key cannot collide.

16. **Payroll daily entry + Sunday week + payroll-to-sales** (`lib/weekUtils.js` rewritten, `hooks/usePayrollConfig.js`, `components/payroll/DayCell.jsx`, `components/payroll/PayrollToSalesCard.jsx`, extended `usePayroll`/`useDashboard`/`HoursView`/`SpeedeeHoursView`/`payrollMath`/`payrollExport`) — the pay week is now **Sunday-first with seven day columns**, and `weekUtils` takes the cutover on every helper because pre-cutover weeks still have to render on the Monday basis. `weekStartOf` resolves Sunday-first then demotes: if the Sunday opening the week falls before the cutover, the date belongs to a Monday week, so no date lands in two weeks or in neither. Both bases end on Saturday, so a week label needs no special case. **The cutover is fetched before either grid mounts** — guessing would draw the wrong columns and then snap. Twenty-one day columns would be unreadable, so one set of seven switches between **Hours / Turned / Other store** while every weekly total stays on screen, and nothing is hidden by the mode. A **technician's day renders read-only with a lock**, per day rather than per row: a part-week technician has some days locked and the rest typeable, which is the per-day resolution made visible. A banner names the Tech Tracker and links to it. Pre-cutover weeks render frozen with a lock notice, matching the database. The roster row carries a **Store manager (salaried)** checkbox on manager rows only. `computePayRow` switched its salaried test to `is_store_manager`, and `entryHours`/`entryTurned` let every formula read either era without knowing which. The **dashboard card** shows the ratio with its components broken out, the window it covered and why, the part-week overtime and guarantee-vs-commission caveats, the technicians-included policy note, the missing-store-manager warning, and the unattributed-days banner mirrored from PR #20. For a pre-cutover week it says a week-to-date figure **cannot exist** — a weekly lump sum cannot be apportioned across days — rather than implying someone forgot to type something. `useDashboard` reads `payroll_week_hours` instead of `timesheet_entries`, since post-cutover an employee can have hours and no weekly row.

17. **Logo, shared date range, dashboard widgets** (`lib/dateRange.js` + `dateRange.test.mjs`, `context/DateRangeProvider.jsx`, `components/DateRangeControl.jsx`, `components/dashboard/RangeWidgets.jsx`, `hooks/useDashboardRange.js`, rewritten `useTechTracker`/`TechTrackerView`, extended `HoursView`/`SpeedeeHoursView`/`ui.jsx`) — **the logo** is the dark-UI master (wordmark lightened for the near-black sidebar, tire left dark) at 80px tall, up from 32px, sitting directly on the surface with the old white plate removed. `width`/`height` attributes carry the intrinsic 480x248 so the header reserves space and does not reflow on load. It renders at 155 CSS px, where the single 480w file gives **3.10x** density — sharp through 3x displays, so the 240w/720w srcset entries would be a bandwidth optimisation, not a sharpness one. **The date range** is one control on three screens, eleven presets, weeks starting **Sunday**, timezone-naive throughout (never `toISOString()`, which shifts a local-midnight date across the UTC boundary and would let two stores disagree about "yesterday"). It lives in `sessionStorage`, so it follows the user between screens and resets to month-to-date on a new session — `localStorage` would carry a stale range across days and quietly answer a different question. 41 offline assertions cover the presets, the month-arithmetic clamps ("last month" from the 31st gives April, not a skipped month) and whole-week coverage. **The Tech Tracker** keeps its five-block layout for a range that is exactly one calendar month and renders a flat day list with a single totals row for anything else; the pay engine sees WHOLE weeks either way, and a week the range clips is labelled. **Payroll** takes the same control: the range chooses which weeks are available and the grid still renders one whole week, so overtime cannot see a fragment of the range by construction. A range spanning the cutover yields Monday weeks before it and Sunday weeks after, with no gap or overlap. **The five widgets** each state the window they cover. **Bonus labelling corrected** per the task: the credit-app penalty copy claimed the waiver "depends on phone conversion, which nothing tracks" — wrong twice, since it has been enterable since migration 27 and there are three independent waiver routes; it now names all three, and the phone-conversion input itself says 40%+ *removes* the penalty and can never create one.

18. **Report Builder** (`lib/reportSpec.js` + `reportSpec.test.mjs`, `lib/reportWorkbook.js`, new shared `lib/excelStyle.js`, `hooks/useReportBuilder.js`, `components/reports/{ReportsView,StoreMultiSelect,MeasurePicker}.jsx`, extended `lib/excelFormats.js`, `App.jsx`, `Shell.jsx`, `lib/closeoutWorkbook.js`) — four choices (the shared date range, stores, measures, one grouping) and a table, exportable to Excel. **Nothing is derived on this screen.** Every number, including the TOTAL row, comes back from `report_build`, so a capture rate in the totals is the whole set's, not the average of the column above it. **The measure list is fetched, not written** — `report_measure_catalog()` supplies it, which is why the four **presets name catalogue GROUPS rather than measure keys**: "Category performance" resolves to 64 measures today and will resolve to 66 the day two categories are added, with no edit here. A preset key the catalogue does not carry is **dropped rather than sent**, because `report_build` rejects an entire request for one unknown measure and losing a column beats losing the report. **The store picker is built from `stores`**, which AuthProvider already loaded under `can_access_location`, so a district manager sees their district and the component never learns what a district manager is; the select-all shortcuts act on the stores present in the list, so "all in region" cannot reach past the caller's scope. **The result carries the spec it was run with.** The table renders `result.columns`, never the live picker — ticking a measure after a run must not slide a header onto a column of numbers it does not describe — and editing any control marks the table stale instead of silently re-labelling it. **Running is a button, not a `useEffect`**: 36 stores by day is not a query to fire on every checkbox click. **The workbook is the cash drawer export's, not a lookalike**: `GOLD`, the thin print border and `applyBorders` moved out of `closeoutWorkbook.js` into a shared `lib/excelStyle.js` that both import, so retuning the accent moves both. `excelFormats.js` gains `HOURS_FMT` and `PERCENT_FMT` in the same three-section, dash-for-zero house style. **Ratios are written as fractions and formatted as percentages** — writing 26.3 under a percent format renders 2630% — and a null ratio stays an EMPTY cell, never a zero somebody later averages. Grouped by store across more than one store, the workbook gets **a tab per store plus the Summary**, and the tabs come from ONE extra call (the same measures by day, split by store) so a tab is a filter of one dataset rather than a separately-derived second answer; if that call would bust the row cap the summary still exports and says why. Verified by reading the generated buffer back with ExcelJS: header fill `FFF5A623`, `$#,##0.00;-$#,##0.00;"-"??` on money columns, frozen header, bold banded TOTAL row, and the "selected stores with no data in this range" block. **Store users are a flag, off** (`STORE_USERS_MAY_BUILD_REPORTS` in `reportSpec.js`), per Part 1 — flipping it grants no reach the database would not have granted anyway, since `can_access_location` already resolves a store user to their own store. **62 offline assertions** cover the null-is-blank/zero-is-a-dash rendering split, preset resolution, the store tree (an orphaned store is shown under "Unassigned", never dropped), column ordering from the catalogue, and Excel tab-name legality.

19. **The five leadership reports** (`lib/reportFormat.js` + `reportFormat.test.mjs`, `components/reports/PriorYearImportModal.jsx`, rewritten `hooks/useReportBuilder.js` and `components/reports/ReportsView.jsx`, extended `lib/reportSpec.js` + tests, `lib/reportWorkbook.js`) — the four guessed presets are gone, replaced by the five reports leadership already circulates. **Each preset fills five controls, not four**: range, stores, measures, grouping AND sort, because every one of these reports is "sorted by" something and the sort is the message — "who sold the most tyres yesterday" is a different question from "the stores, alphabetically". Clicking any column header re-sorts, descending first. **Two reports read two windows at once** (`altRange` / `altMeasures`), and the column header carries a badge naming the other window, because a *yesterday* column sitting silently beside month-to-date columns is how somebody quotes the wrong number in a meeting. **`requiresPriorYear` is declared per preset** and the test suite checks the inverse too — any prior-year measure a report uses must be declared, or it would render blank instead of **n/a**, and blank reads as "nothing happened" rather than "we cannot answer this yet". With `prior_year_actuals` empty those columns show `n/a`, the header says *no 2025 data*, and a banner explains why; **never zero, and never a percentage against nothing**, which would read as infinite improvement for every store in the company. **Conditional formatting is applied, never authored, here** — `lib/reportFormat.js` knows how to evaluate a rule and what a colour token looks like, and nothing in the frontend knows a threshold. A rule never fires on a blank: an absent value is not a low value, and colouring an unreported store red would say something false about it. `between` is inclusive at the bottom and exclusive at the top so adjacent bands cannot both claim their shared boundary. **Two palettes, one token**, the same split migration 30 introduced with `export_argb`: the portal's theme is dark, so `--success-tint` is a near-black green that is right behind pale text and unreadable as a fill in a white Excel sheet, so each token carries a screen pair and a separate light ARGB. The workbook applies fills **per cell**, because a rule matches a value rather than a column and one column can carry three bands; a rule's label goes in as a STRING in a money column, which is correct — the number stopped being the point once the tier was cleared. **The prior-year import takes a paste**, headers and all, tab- or comma-separated, tolerating `$` and commas, because BDC will send a spreadsheet and nobody should have to produce JSON or match a uuid. **31 new offline assertions** for the rule engine and the preset suite rewritten around the five reports — that every sort measure is a column the report actually shows, that every alt measure is one of the report's own columns, that a report with alt measures always names an alt range, and that no prior-year column slips through undeclared.
20. **Company Directory** (`components/directory/{DirectoryView,DirectoryCards,DirectoryEditors}.jsx`, `hooks/useDirectory.js`, `lib/directory.js` + `directory.test.mjs`, extended `Shell.jsx`, `App.jsx`, `ConfirmDialog.jsx`) — a **Directory** nav item for every role; the view is not keyed to `currentStore` because it is not store-scoped. Two tabs, one search box that filters whichever is active, region chips on both. **Stores** group by region, then district, then store number; each card shows number, name, brand badge, shop number, address, `tel:` main phone, weekly hours (consecutive identical days collapse to "Mon–Fri"; never-entered hours say so instead of rendering closed), the hours note, and the store manager and DM **derived from coverage** — a store manager is an active `store_manager` contact with active store coverage, a DM an active `district_manager` with active district coverage, so a DM who also covers a store is not listed as its manager. Unassigned reads "Not assigned" in muted text, never a warning colour. **People** list active contacts sorted by category then `sort_order` then name, with `tel:`/`mailto:` links (an extension is dropped from the `tel:` href) and plain-language coverage ("Columbia East district", "Store 3935 Two Notch", "All stores"); search also matches coverage text, and a company-wide contact passes every region filter. Names on store cards jump to the person, store references on people jump to the card — the jump clears filters that could hide the target, scrolls, focuses and briefly rings it. **Admin/master** get Edit on store cards (address, phone, a per-day hours editor with "Copy Monday to Tue–Fri", hours note), Edit/Deactivate on people (Deactivate confirms; Reactivate does not), Add contact, and a "Show deactivated" toggle. **The hook reads only directory sources** — `directory_stores()`, the two directory tables (explicit columns, never `employee_id`), `districts`, `regions` — verified live from the resource log: no request to `employees`, `employee_pay_rates` or `locations`. No employee picker in v1 for exactly that reason. **Phones had no navigation at all** (the sidebar is `hidden md:flex`), so `Shell.jsx` gained a `md:hidden` screen select in the header; verified at 375px with no horizontal overflow. `ConfirmDialog` gained a `busyLabel` prop (default unchanged). 50 offline assertions.

21. **Employee profile** (`components/payroll/EmployeeProfilePanel.jsx`, `hooks/useEmployeeProfile.js`, `lib/payRates.js` + `payRates.test.mjs`, extended `usePayroll`/`HoursView`/`SpeedeeHoursView`/`TechTrackerView`) — every name on Payroll (both brands) and the technician's name on the Tech Tracker opens a side panel. **Details** (name, position, hire date, Employee / ADP ID, and for admin/master the salaried-store-manager switch) are editable by anyone who can see the store, as `employees` RLS already allowed. **Employment**: "End employment" takes a last day worked (not in the future, not before hire) and sets it with `active = false`; the person stays on every pay week through that day; admin/master can Reactivate. It replaces the grid's old Remove, whose trash icon now opens the profile. **Pay** (admin/master only; nothing is fetched otherwise — verified live, no rate request leaves a store user's browser): the rates in force this pay week, a Change-pay form (type, amount, takes-effect date) that names the first pay week the change applies to and warns only when it would REPLACE an existing rate for weeks already started, and the full history with a withdraw control on profile-entered rows. The default takes-effect date is this week for a first rate and next week for a change, so an ordinary raise never re-prices a week in progress by accident. A technician's flat/guarantee are shown from the Tech Tracker and the flat type is locked. **The grid's rate cells are read-only** and show the week's rate; clicking opens the profile. Adding an employee opens their profile immediately. `usePayroll` now fetches the whole roster and shows a person on a week if they have data in it or were employed during it (mirrors the SQL). 30 offline assertions.

22. **Directory v1.1 frontend** (extended `lib/directory.js` + tests, `hooks/useDirectory.js`, `components/directory/{DirectoryCards,DirectoryEditors,DirectoryView}.jsx`) — store cards gain a **Marchex** line under the main number and a row of **service chips**; both numbers render through `formatPhone()` as `(803) 407-1911` while the database keeps the ten digits BDC supplied, and the `tel:` link is unchanged. Search now matches service names, so "alignments" finds the stores that offer them. The admin store form gains the Marchex field and a checkbox per service; saving calls `directory_update_store` then `directory_set_store_services`. A **Service types** button on the Stores tab opens the catalogue: rename a type (its code is fixed and the database refuses to change it), switch one off (stores keep pointing at it), or add one — the code is derived from the name and shown before saving. Non-admins see none of it, and a store user's page requests only directory sources plus `service_types`. 59 offline assertions.

23. **Store email + a validation bug the field exposed** (`components/directory/{DirectoryCards,DirectoryEditors}.jsx`, `hooks/useDirectory.js`, `lib/directory.js`) — the store card gains an email line (a `mailto:` link, or "Email not entered"), the admin form gains a **Store email** field, and search matches the address. **The bug:** that field carries `type="email"`, and the BROWSER's native validation blocked the form's submit before React ever ran — so an invalid address left the dialog sitting there with no message, no save and no explanation. The contact form's work email had the same flaw. Both forms now set `noValidate`, so the inline messages already written are the ones that speak, while the inputs keep `type="email"`/`"tel"` for the right mobile keyboard. **Worth remembering: an inline validation message is dead code if the browser's own validation fires first.**

24. **Hours removed from the directory frontend** (`components/directory/{DirectoryCards,DirectoryEditors}.jsx`, `hooks/useDirectory.js`, `lib/directory.js` + tests) — the card's hours block and the form's seven-day editor, the "Copy Monday to Tue–Fri" helper and the hours note are all gone. The library's hours helpers (`DAYS`, `formatTime`, `hoursRows`, `hoursToForm`, `formToHours`, `hoursFormErrors`) and their assertions went with them rather than being left as dead code — nothing else imported them, and git history holds the shape if hours ever come back. 48 offline assertions remain.

25. **Contact photos** (`Avatar` + `PersonCard` in `DirectoryCards.jsx`, `PhotoField` in `DirectoryEditors.jsx`, `hooks/useDirectory.js`) — a round photo beside each name on the People tab, or **their initials** when there is none. Deliberately not a grey silhouette: initials read as "no photo yet", a silhouette reads as an unknown person. Photos are fetched as a batch of signed URLs (one hour) whenever the directory loads, because the bucket is private. Admins get Upload / Replace and a Remove that asks first, since removing deletes the file; the picker and the hook both check type and size before the bucket does. **The photo saves on choosing, not on Save**, and is offered only once the contact exists -- a photo needs somebody to belong to. **Replace order matters and is deliberate**: the new file is uploaded, the row is pointed at it, and only then is the old file removed, so no failure can leave a contact pointing at a file that is gone; if the row update is refused the just-uploaded file is cleaned up. Also fixed: `Avatar`'s initials regex had lost its backslash in generated code, so every fallback showed ONE letter ("J" for John Dieu) -- the same escaping slip as the phone formatter earlier.

## Bugs found and fixed along the way

- **`Card` didn't forward `onClick`** (`ui.jsx`) — broke the Add User modal's click-outside-to-close, since every click inside the modal (including the email field) bubbled to the backdrop and closed it. Fixed by spreading `...rest` props onto `Card`'s div.
- **Vercel env vars never applied to Production** — they were added but no redeploy was triggered (Vite bakes `VITE_*` vars in at build time). Fixed by redeploying after confirming the vars were set.
- **Supabase Site URL still `localhost`** — new-user confirmation emails redirected to a dead `localhost` URL instead of the production app. Needs fixing in Supabase dashboard → Authentication → URL Configuration (Site URL + Redirect URLs should be `https://pgwsupport.vercel.app`). Confirmed the email *confirmation itself* still succeeds server-side even with the bad redirect — affected users can just go straight to the login page and sign in.
- **`@pgwsupport.com` is not a real domain** — it was only ever placeholder text in the login screen's example (`you@pgwsupport.com`). Real company domain is `pgwus.com`. Supabase's signup validation rejects the fake domain; several `*_TEST@pgwsupport.com` accounts in the users list were inserted directly via SQL (bypassing that validation), not through real signups.
- **User Management row state didn't resync after save/revoke** — a row's local role/scope dropdowns kept showing stale values until a full page reload. Fixed with a `useEffect` that resyncs from the server value.

## Known gaps / things not yet built

- **The Horizon upload transport is not built yet, but the protocol is now known.** It is not a file upload: each store's monthly `.xlsm` workbook runs a macro (`Module1.SendDataToServer`) that reads the hidden "Excel Uploader" sheet and POSTs `application/x-www-form-urlencoded` to `https://coaching.horizontmg.com/importer/coaching.php`. Fields: `data[SHOP_STORENUMBER]`, `data[PASSWORD]`; `data[kpi][<days since 1970-01-01>][<key>]` for every row key, per day from the 1st through today (capped at month end); `data[monthly][<first-of-month days>][kpi_su_*]` = the month's penetration goal %; `data[monthly][...][kpi_tech_N_name]` for N = 1–12. It must run server-side (an Edge Function), so the per-shop password never reaches a browser, and it must take the shop number only from `horizon_upload_target()`. **Every store workbook carries its shop's Horizon password in plain text (Excel Uploader!B2)**, so whoever receives a workbook receives the password. Credential storage is done (migration 41, Vault); the sandbox holds Millwood's July (migration 42); the Edge Function `horizon-upload` is deployed with preview and send (entries 46–47); the first sandbox upload succeeded on 2026-09-16 (entry 48, shop `B306006`) and store, district and regional managers now have a Send to Horizon button on the Daily Tic Sheet (entries 49–50); before real stores it must decide how a past month finds who held each slot that month. Millwood's July workbook also lists **Rick Schroeder at tech 12**, who is not in the portal.
- No delete-user (only revoke-access, which keeps the Supabase Auth login intact but strips all access) — a true delete would need a `service_role` key in a serverless function, deliberately avoided so far.
- No password-change / forgot-password UI for end users yet.
- Custom domain (`pgwsupport.com`) not configured on Vercel — deliberate, per instruction to test on the temporary URL first.
- Google review counting is **on hold pending a decision** — the manual entry field from Task 6 stays; no automatic counting and no widget were built, deliberately.
- The brief's "Upgrade once live" export items are **covered for reporting** by the Report Builder (cross-store, any date range, real `.xlsx` via ExcelJS). What is still not built is a cross-store/date-range export of the **payroll grid itself** — `payrollExport.js` remains one store, one week.
- **THE PORTAL HAD TWO GROSS PROFITS — RESOLVED 2026-09-17 (migration 49).** They differed by exactly the Adjustments (formerly Groupon) amount: `lib/grossProfit.js` (Summary R35) includes it split 50/50 across labour and parts and is what `useBonusTracker.js` pays managers on; `dashboard_range_metrics` (migration 33) left it out and said so in its own header. **The user decided the figure that INCLUDES Adjustments is correct**, so migration 49 changes `dashboard_range_metrics`'s `gross_profit` to Sales + Adjustments - cost of sales and its `gross_profit_pct` to that over Sales + Adjustments — the Report Builder's definition, which the dashboard now equals to the cent. `gross_sales` still holds the TIC SHEET Sales (Adjustments excluded); the Sales widget and payroll-to-sales are untouched, so the dashboard shows Sales on one tile and a gross profit over a slightly larger base on the next. The GP tile now says "of sales + adjustments" and lists the Adjustments included.
- **Saved reports are not built** — deliberately out of scope per Task 10, which names them as the natural follow-on. A report is rebuilt from its controls each time; nothing is persisted.
- **The Report Builder's district scoping was verified at STORE scope.** No district-role account exists, so the live checks ran as `teststore@pgwsupport.com` (role `store`, #3303): a crafted request for another store returned zero rows, `[own, other]` returned only own, all four pay measures returned 42501, and aggregate labour cost still came back. That exercises the same `can_access_location` code path a district user takes, but the *district* case itself is unproven until a district login exists.
- **ExcelJS is not actually code-split**, despite comments in `useCloseoutRangeExport.js` saying it is. Verified at commit `9287e99`, i.e. it predates the Report Builder: `npm run build` puts it in the entry chunk, not the `closeoutWorkbook` chunk, so every user downloads roughly a megabyte on first paint. Needs a `manualChunks` rule in `vite.config.js`; tracked separately from Task 10.
- **No store manager is flagged at any store yet**, because no real rosters exist (9 employees across 3 of 36 stores). The payroll-to-sales exclusion therefore ships *inert*. The risk is not the migration — it is whoever enters 36 rosters later typing GMs as `position = 'manager'` and leaving `is_store_manager` false, which would put a salaried GM's pay in every store's numerator. The widget's `missing_store_manager` warning exists to catch exactly that, and it is the thing to watch when rosters are built.
- **Payroll-to-sales covers post-cutover weeks only.** It reads daily rows; a pre-cutover week holds one weekly total per person and a week-to-date figure cannot be derived from it. This is a property of the metric, not a gap to close.
- SpeeDee payroll remains **entirely unexercised** — no SpeeDee store has a single employee. Its day columns were built alongside Midas's because `payroll_daily` is brand-agnostic, but nothing has run against them.
- Local dev sessions in the Browser pane tool have repeatedly lost `localStorage` (and thus the login session) between turns — not an app bug, just a quirk of the sandboxed browser tool; re-login is needed each time this happens.

- **`prior_year_actuals` is LOADED: 393 rows, all 36 stores, 2025.** Imported 2026-08-27 from the handout export (sales $49,405,892 · gross profit $26,337,497 · cars 182,037). Every row was verified against the source file individually, not just in total — a totals-only check survives a store-to-store swap. Four stores (Duke St, Fairfax, Manassas, Rhode Island) carry only Oct–Dec and one (SpeeDee James Island) starts in March, so a 2026 month outside those windows correctly has no comparison rather than a false one. **The handout names two stores differently from the portal**: "Lexington" is #3276 (portal: Midas North Lake) and "Wesmark" is #3938 (portal: Midas Sumter); "SpeeDee – Lexington" is the separate #3308. Both mappings were confirmed with BDC before loading.
- **#3938's 2025 sales and gross profit are DERIVED, not measured — and are loaded anyway, per BDC.** Its gross profit is exactly 0.39245 × sales in all twelve months (a real store wanders: Millwood ran 0.619 → 0.536), and its sales-per-RO sits in a 0.3% band, 286.93–287.87, where Millwood's spans 278–397. Capture rate is 100.0 and tyre units 0 every month. Only its RO counts vary normally and look real. This was raised with the evidence and the decision was to load all three columns; it is recorded here so nobody later reads #3938's vs-2025 comparison as a measurement. Note this is the same store whose bonus targets were column-shifted and fixed in migration 28 — a second questionable dataset for one store is a pattern worth a look at the source.
- **Two conditional-formatting rules are PLACEHOLDERS, marked as such in migration 36.** The percent-of-budget cut points are seeded at 100% / 90% because those are the obvious round numbers, **not** because anyone confirmed them. The store-name fill by market is **not seeded at all** — five invented colours would look authoritative while being made up; the mapping is five inserts once BDC says which colour is which. Correcting either is an `update`, not a deploy.
- **THE SAMPLE AND THE DATABASE DISAGREE ABOUT MARKET ASSIGNMENT.** Task 10's Market Review sample shows five markets covering 35 stores, with SpeeDee Lexington sitting outside them and explaining a $6,359 gap between the market sum and the PGW total. In this database **all 36 stores are assigned** — Charleston 10, North 8, Columbia East 6, Columbia West 6, Florida 6 — and **#3308 SpeeDee Lexington is in Columbia West**. Verified live: the market projections reconcile to the company total with a gap of exactly 0, which is what must happen when every store belongs to a market. Nothing was reassigned. The gap needs another explanation from BDC. (There are also four SpeeDee stores, not one.)
- **Pace still lives in three JavaScript implementations plus the new SQL one.** `report_pace()` is authoritative for reports; `lib/ticSheetMath.js` `paceOf()` and `lib/bonusMath.js` compute the identical formula for the tic sheet and the bonus tracker, and `useMonthlyGoals.js` computes a *different* metric that happens to be called pace ("daily pace needed" = remaining ÷ days left). Consolidating the first two onto the SQL function was **deliberately not done inside a reporting task** — both screens are shipped and verified, the formula is identical, and rewriting them here would trade real regression risk for tidiness. Worth doing on its own.
- **`days_elapsed` has two definitions and the portal now publishes both.** "Days Entered (any data)" counts a date whose tic-sheet row has any non-zero content (migration 33's rule, and what tyres-per-day divides by); "Days Traded (ROs)" counts a date with `ro_count > 0` (the tic sheet's PACE row and the bonus figures, and what every projection here divides by). They disagree for a day where someone typed sales but no repair orders. Publishing one while quietly using the other is how a report becomes unreconcilable with the screen it came from, so both are selectable and labelled. Which one BDC actually means is still open.
- **The Company Directory has no real data yet.** Addresses were backfilled from the migration-3 seed; phones exist only for Oviedo and Semoran; hours and contacts are empty. **Decided by the user 2026-09-18: there will be NO seed migration — admins enter directory data by hand** through the page (Edit on store cards, Add contact on the People tab). Nothing in the UI sets `directory_contacts.employee_id`, so every contact will have it null; the column stays for a future link but nothing reads it. The test rows from verification were removed and confirmed (0 contacts, 0 coverage rows, Millwood's phone/hours/note null). Holiday hours are out of scope for v1 (`hours_note` covers ad-hoc notes). The portal has **no URL routing** (the brief's `/directory` is a view key) and **no light theme** (the directory uses only theme tokens, so it follows one if added).

## How to verify things still work

Master login: `hallen@pgwus.com` (password known to the user). After signing in, all tabs should be visible (Dashboard, Training, Cash Drawer, Daily Tic Sheet, Payroll, Employee Schedule, Documents, and Users for master) with the store picker showing all 36 stores.

For the Daily Tic Sheet specifically: on a Midas store the Service Units grid shows exactly 29 categories in `display_order`; enter numbers → Save → reload shows the same values; a previous date loads that day's data. Cross-store isolation and the master-only reference tables are enforced by Postgres RLS (verified: a store user gets a 403 `42501` inserting another store's `daily_kpi`, and cannot write `service_categories`).
